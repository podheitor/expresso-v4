# Expresso v4 — Ponto de Retomada

**Último sprint commitado:** #550 (2026-05-03)

```
git log --oneline | head -15
```

---

## O que foi feito nesta sessão (#336–#347)

| Sprint | Escopo | O que foi feito |
|--------|--------|-----------------|
| #336 | calendar | Last-Modified + IMS em `export.ics`; ETag + LM + INM + IMS em `itip/request.ics` |
| #337 | drive | Last-Modified + IMS em `GET /quota` via `MAX(updated_at)` drive_files |
| #338 | mail | ETag + INM em `HEAD /messages/:id/raw` — mesmo formato que GET /raw |
| #339 | mail | ETag + INM em `GET /messages/:id/attachments` — imutável como raw |
| #340 | mail | Last-Modified + IMS em `list_folders` e `list_all_folders` via `MAX(updated_at)` mailboxes |
| #341 | mail | Last-Modified + IMS em `GET /quota` via `MAX(received_at)` messages |
| #342 | search | Parâmetro `offset` em `GET /api/v1/search` — paginação via `TopDocs::and_offset()` |
| #343 | meet | `PATCH /api/v1/meetings/:id` — atualiza title/schedule/lobby/password (moderator-only) |
| #344 | drive | `PATCH /api/v1/drive/files/:id/metadata` — renomear arquivo/pasta sem reupload |
| #345 | drive | `POST /api/v1/drive/files/:id/move` — mover arquivo/pasta para outro diretório |
| #346 | drive | `POST /api/v1/drive/files/bulk-move` — mover até 200 itens atomicamente |
| #347 | meet | `POST /api/v1/meetings/:id/restore` — reativa reunião arquivada (creator ou moderador) |
| #348 | meet | `GET /api/v1/meetings?archived=true` — lista reuniões arquivadas do usuário |
| #349 | drive | `POST /api/v1/drive/files/:id/copy` — cópia shallow: nova row, mesmo blob |
| #350 | drive | `POST /api/v1/drive/files/bulk-trash` — soft-delete até 200 itens em batch |
| #351 | meet | `DELETE /api/v1/meetings/:id/participants/:user_id` — remover participante (moderator-only) |
| #352 | drive | `GET /api/v1/drive/users/:user_id/usage` — bytes usados por usuário no tenant |
| #353 | meet | `PATCH /api/v1/meetings/:id/participants/:user_id` — promover/rebaixar role (moderator-only) |
| #354 | drive | `GET /api/v1/drive/files/search?q=` — busca por nome ILIKE no tenant |
| #355 | meet | `GET /api/v1/meetings/:id/participants/:user_id` — detalhe de participante com ETag/LM |
| #356 | drive | `GET /api/v1/drive/files?kind=file\|folder` — filtro por tipo na listagem |
| #357 | meet | `GET /api/v1/meetings/:id/participants/count` — contagem de participantes |
| #358 | drive | `GET /api/v1/drive/files?sort=name\|updated_at\|created_at\|size_bytes&order=asc\|desc` |
| #359 | meet | `PATCH /api/v1/meetings/:id` — campo `is_recurring` adicionado ao `UpdateBody` |
| #360 | drive | `GET /api/v1/drive/files?limit=N&offset=N` — paginação na listagem (limit 1–500, default 200) |
| #361 | meet | `GET /api/v1/meetings?after=&before=` — filtro por `scheduled_for` em RFC 3339 |
| #362 | drive | `GET /api/v1/drive/files/search?q=&limit=&offset=` — paginação na busca por nome |
| #363 | meet | `GET /api/v1/meetings/:id/participants?limit=N&offset=N` — paginação na lista de participantes |
| #364 | imap | NAMESPACE (RFC 2342) — anuncia namespace pessoal prefix="" delim="."; feature `ext_namespace` habilitada |
| #365 | notifications | Redis pub/sub cross-pod — `internal_notify` publica em `"expresso:notifications"`; outros pods recebem via relay subscriber |
| #366 | imap | CONDSTORE/QRESYNC (RFC 7162) — `mod_sequence` em `messages`, `HIGHESTMODSEQ` em SELECT/STATUS, `CHANGEDSINCE` em FETCH, `MODSEQ` data item, `ENABLE CondStore` |
| #367 | drive | `GET /api/v1/drive/files/:id/preview` — `Content-Disposition: inline` para image/* e application/pdf; 415 para outros tipos |
| #368 | meet | Webhook on meeting created/archived — `MEET__WEBHOOK_URL` fire-and-forget POST `{event, tenant_id, meeting}` |
| #369 | compliance | `GET /api/v1/compliance/archive/export` — ZIP em memória com `manifest.json` + `messages/*.eml`; mesmos filtros que list |
| #370 | meet | Webhook on meeting restored — `meeting.restored` event via `webhook::dispatch` no handler `restore` |
| #371 | compliance | Export ZIP signed — HMAC-SHA256 em `X-Export-Signature` via `COMPLIANCE__EXPORT_SECRET` (hmac+sha2+hex) |
| #372 | notifications | Metrics — `IntCounterVec notifications_dispatched_total{kind}` incrementado em `internal_notify` |
| #373 | drive | `DELETE /api/v1/drive/files/:id/versions/:v` — remove versão específica + blob em disco |
| #374 | drive | `POST /api/v1/drive/files/bulk-copy` — shallow-copy até 200 itens com nome "<original> (cópia)" |
| #375 | meet | Webhook retry — exponential backoff até 3 tentativas (1s/2s/4s) em `webhook::dispatch` |
| #376 | drive | `POST /api/v1/drive/files/bulk-restore` — desfaz trash de até 200 itens atomicamente |
| #377 | compliance | Export ZIP password — AES-256 via `?password=` em `GET /api/v1/compliance/archive/export` |
| #378 | notifications | Webhook externo — `NOTIFICATIONS__WEBHOOK_URL` dispara POST fire-and-forget em `internal_notify` |
| #379 | drive | File tags — `GET/POST /api/v1/drive/files/:id/tags` + `DELETE /:id/tags/:tag` — tabela `drive_file_tags` |
| #380 | meet | Recording — `POST /api/v1/meetings/:id/recording/start|stop` — campo `recording_started_at`; `MeetError::Conflict` |
| #381 | search | Facets — `GET /api/v1/search?facet=kind` retorna `facets.kind[]` via `DocSetCollector` + campo `kind` no schema |
| #382 | drive | File share link — já implementado (detectado); sprint pulado |
| #383 | mail | Scheduled send — `POST /api/v1/mail/messages/schedule` com `deliver_at TIMESTAMPTZ`; background worker 30s |
| #384 | meet | Chat integration — `GET /api/v1/meetings/:id/chat` retorna metadados do canal via `chat_channels` |
| #385 | notifications | Digest — `GET /api/v1/notifications/digest?since=` agrega não-lidas por kind; tabela `notifications` persistida |
| #386 | drive | Folder download — `GET /api/v1/drive/folders/:id/download` — ZIP recursivo; `collect_files_recursive` em `FileRepo`; crate `zip` adicionado |
| #387 | mail | Vacation toggle — `PATCH /api/v1/mail/vacation/toggle` com `is_active`; preserva outros campos; re-renderiza sieve_script |
| #388 | meet | Waiting room — `GET /api/v1/meetings/:id/lobby` + `POST /approve/:user_id` + `DELETE /:user_id`; tabela `meeting_lobby` |
| #389 | search | Delete by tenant — `DELETE /api/v1/index?tenant_id=` remove todos docs via `delete_term` no Tantivy |
| #390 | drive | File expiry — `PATCH /api/v1/drive/files/:id/expiry`; campo `expires_at`; worker GC horário purga blobs expirados |
| #391 | mail | Read receipts — `POST /api/v1/mail/messages/:id/read-receipt` envia MDN RFC 8098 multipart/report |
| #392 | meet | Poll/vote — `POST/GET /api/v1/meetings/:id/polls` + `GET/DELETE /:poll_id` + `POST /vote`; tabelas `meeting_polls` + `meeting_poll_votes` |
| #393 | notifications | Mark read — `PATCH /api/v1/notifications/:id/read` + `/read-all`; UPDATE is_read=true |
| #394 | compliance | Retention policy — `GET/PUT /api/v1/compliance/retention`; tabela `compliance_tenant_retention`; default 365 dias |
| #395 | drive | File lock — `POST/DELETE /api/v1/drive/files/:id/lock`; campos `locked_by + locked_at`; lock otimista por usuário |
| #396 | mail | Flag presets — `GET/POST /api/v1/mail/flag-presets` + `GET/PUT/DELETE /:id`; tabela `mail_flag_presets` com flags JSONB |
| #397 | meet | Breakout rooms — `POST/GET /api/v1/meetings/:id/breakouts` + `GET/DELETE /:room_id` + `POST/DELETE /:room_id/participants`; tabelas `meeting_breakout_rooms` + `meeting_breakout_participants` |
| #398 | contacts | Import CSV — `POST /api/v1/contacts/import`; multipart (book_id + file); parser CSV quoted; gera vCard 3.0 por linha |
| #399 | — | Pulado — calendar event RSVP já implementado (detectado em `events.rs`) |
| #400 | drive | Starred files — `POST/DELETE /api/v1/drive/files/:id/star` + `GET /api/v1/drive/starred`; campo `starred_at` |
| #401 | calendar | Event alarms — `GET/POST /api/v1/calendars/:cal_id/events/:event_id/alarms` + `DELETE /:alarm_uid`; tabela `calendar_event_alarms(uid, action, trigger_rel, trigger_abs, description)`; `CalendarError::AlarmNotFound` |
| #402 | — | Pulado — mail thread view já implementado (`/mail/threads/:thread_id` + `list_thread` + `thread_id` filter em `list_messages`) |
| #403 | notifications | Push subscription — `POST/DELETE /api/v1/notifications/push`; tabela `notification_push_subscriptions(endpoint, p256dh, auth)`; UPSERT por endpoint |
| #404 | meet | Transcript metadata — `GET/POST /api/v1/meetings/:id/transcript`; tabela `meeting_transcripts(url, language, starts_at, ends_at, created_by)`; GET para participantes; POST moderator-only |
| #405 | drive | Comments — `GET/POST /api/v1/drive/files/:id/comments` + `DELETE /:comment_id`; tabela `drive_file_comments(user_id, body)`; DELETE restrito ao autor |
| #406-#485 | vários | Roadmap continuado (search/calendar/mail/meet/drive/contacts/compliance/notifications) — ver `git log --oneline` ou `memory/project_status.md` para detalhes |
| #486 | compliance | Archive tag co-occurrence — `GET /api/v1/compliance/archive/tags/co-occurrence` (paralelo drive #484, USER-scoped) |
| #487 | mail | Mark-unread bulk per special-use — `POST /api/v1/mail/folders/special-use/mark-unread?slots=trash,junk` (combo #485 + #473) |
| #488 | calendar | Event instance EXDATE cancel — `POST /:cal_id/events/:id/cancel-instance {instance}` injeta EXDATE; expander filtra via `parse_exdates` |
| #489 | drive | Tag intersect-exclude — `GET /api/v1/drive/tags/intersect-exclude?tags=&exclude=` (AND-set + NOT EXISTS) |
| #490 | mail | Folder rename revert-all batch — `POST /folders/rename-history/revert-all?n=N` (DISTINCT ON + atômico) |
| #491 | calendar | EXDATE list/clear/delete — `GET /:id/exdates` + `DELETE /:id/exdates` + `DELETE /:id/exdates/:instance` (inverso completo de #488) |
| #492 | compliance | Archive tag intersect-exclude — `GET /archive/tags/intersect-exclude?tags=&exclude=` (paralelo USER-scoped de #489) |
| #493 | drive | Tag co-occurrence by user — `GET /tags/co-occurrence-by-user?user_id=&tag=&min_count=` (extensão #484 + #479) |
| #494 | mail | Folder rename revert-all dry-run — `?dry=true` em `revert-all` (preview SELECT-only) |
| #495 | calendar | RECURRENCE-ID instance override — `POST /:id/override-instance {instance, summary?,...}` (alternativa não-destrutiva ao EXDATE cancel #488) |
| #496 | calendar | RECURRENCE-ID override list — `GET /:id/overrides` lista os VEVENT overrides existentes (paralelo ao #491 EXDATE list) |
| #497 | calendar | RECURRENCE-ID override delete — `DELETE /:id/overrides/:recurrence_id` remove um override (inverso de #495) |
| #498 | calendar | RECURRENCE-ID override patch — `PATCH /:id/overrides/:recurrence_id` edita summary/description/location/dtstart/dtend in-place (complemento de #495+#496+#497) |
| #499 | calendar | EXDATE/RECURRENCE-ID conflict-aware — 409 em `override-instance` se EXDATE pra mesma instance e em `cancel-instance` se override existe; `Conflict` enum agora carrega `String` |
| #500 | calendar | RECURRENCE-ID override get-one — `GET /:id/overrides/:recurrence_id` snapshot completo (summary/description/location/dtstart/dtend/dtstamp) com ETag/LM herdados do master, 304 em INM/IMS, 404 se não existe |
| #501 | calendar | Override→cancel migration — `POST /:id/overrides/:recurrence_id/cancel` substitui workflow 2-passos (DELETE override + POST cancel-instance) por 1 chamada atômica; 1 só DB write, sequence bumpa 1 vez |
| #502 | calendar | Cancel→override migration — `POST /:id/exdates/:instance/override {summary?,description?,location?,dtstart?,dtend?}` inverso simétrico do #501; `remove_exdate_value` + `inject_before_end_vcalendar` compostos antes do único `update`; 1 DB write, sequence bumpa 1 vez; 404 se EXDATE não existe; 409 se override já existe; 400 se sem rrule ou sem campos override |
| #503 | calendar | Override list rica — `GET /:id/overrides?detail=full` adiciona description+location em cada item pra paridade com get-one (#500); default `summary` mantém shape original do #496; helper `list_recurrence_id_overrides` ganha bool `full`; refator pra `upper16` uniforme (universal prefix matcher); 400 em detail desconhecido |
| #504 | calendar | EXDATE list rica — `GET /:id/exdates?detail=full` (paralelo simétrico do #503); novo helper `parse_exdates_rich` cobre TZID/parametros/date-only/unknown que `parse_exdates` plain ignora; cada item ganha `tzid?`, `params?`, `kind`(`"utc"\|"tzid"\|"date-only"\|"unknown"`), `raw_value`; default `summary` preserva shape `{compact,rfc3339}` do #491; 400 em detail desconhecido |
| #505 | calendar | Override DTSTAMP-only touch — `POST /:id/overrides/:recurrence_id/touch` refresca SÓ o DTSTAMP do VEVENT override sem mutação de campos; reusa `patch_recurrence_id_override_block` com TODOS os campos None; sequence NÃO bumpa (DTSTAMP fora das colunas DISTINCT FROM) mas ETag/updated_at do master refrescam — bastante pra invalidar HTTP/CalDAV cache; sem body; retorna `{event_id, recurrence_id, touched:true, dtstamp, etag, sequence}`; 404 se override não existe |
| #506 | calendar | Master event DTSTAMP-only touch — `POST /:id/touch` paralelo do #505 mas no master VEVENT; novo helper `patch_master_dtstamp` matcha bloco com `UID==master AND !has_recurrence_id` (overrides com mesmo UID preservados); se DTSTAMP ausente no master, adiciona; mesma semantics do #505 (sequence NÃO bumpa, ETag/updated_at refrescam); use case: forçar re-sync em clients iCal cacheando por DTSTAMP, "ressuscitar" eventos pós-restore; sem body; retorna `{event_id, touched:true, dtstamp, etag, sequence}`; 400 se master sem UID; requer WRITE+ |
| #507 | calendar | Override DTSTAMP-only touch BULK — `POST /:id/touch-overrides {"instances":[…]}` variante batch do #505; valida cada instance via `has_recurrence_id_override` separando `touched` vs `not_found` (best-effort, não 404 individualmente); aplica `patch_recurrence_id_override_block(..., None×5)` sequencialmente in-memory; 1 único `EventRepo::update` no fim (vs N round-trips); dedup por `target_compact`; limite 1..256 instances; 404 só se NENHUMA bate; mesma semantics do #505 (sequence NÃO bumpa, ETag/updated_at refrescam); use case: ressuscitar série inteira após bug de sync sem N requests; retorna `{event_id, touched, not_found, dtstamp, etag, sequence}`; requer WRITE+ |
| #508 | calendar | Master+overrides touch-all — `POST /:id/touch-all` combina #506 + #507; descobre overrides via `list_recurrence_id_overrides(raw, uid, false)` (mesmo walker do #503), itera extraindo `compact` e aplica `patch_recurrence_id_override_block(..., None×5, &dtstamp_now)` in-memory; depois `patch_master_dtstamp(raw, uid, &dtstamp_now)` no fim; 1 único `EventRepo::update`; cache-nuke total do VCALENDAR sem cliente listar nada; mesma semantics #505/#506/#507 (sequence NÃO bumpa, ETag/updated_at refrescam); sem body; 400 se master sem UID; retorna `{event_id, master_touched:true, overrides_touched:[…compact…], dtstamp, etag, sequence}`; requer WRITE+ |
| #509 | calendar | Override touch by range — `POST /:id/touch-overrides-by-range?after=&before=` variante range do #507 sem listar instances; descobre overrides via `list_recurrence_id_overrides`, parseia cada `compact` via `parse_one_exdate`, filtra `[after, before)` (half-open, ambos opcionais — sem nenhum ≡ #508 sem master); aplica `patch_recurrence_id_override_block(..., None×5)` in-memory; 1 único `EventRepo::update`; mesma semantics #505 (sequence NÃO bumpa, ETag/updated_at refrescam); 400 se `after >= before`; 404 se nenhum override no range; retorna `{event_id, touched:[…compacts…], skipped:[…fora do range…], dtstamp, etag, sequence}`; use case: ressuscitar só overrides futuros sem afetar histórico, ou janela de migração específica; requer WRITE+ |
| #510 | calendar | Touch-all dry-run — `?dry=true` no #508 retorna o plano (lista de compacts que SERIAM tocados + master:true) sem chamar `EventRepo::update`, sem alterar ETag/updated_at/DTSTAMP, sem publicar `EventUpdated`; nova `TouchAllQuery` struct + `Query<TouchAllQuery>` extractor no `touch_all`; quando `dry`, walka `list_recurrence_id_overrides` igual ao path real mas só popula `overrides_touched` sem patches; retorna `{dry:true, event_id, master_touched:true, overrides_touched:[…]}` (sem etag/sequence/dtstamp); 400 ainda fired se master sem UID; default `dry=false` preserva semantics original; útil pra UI confirmar "vai mexer em N overrides + master, ok?" antes de cache-nuke; paralelo do #494 (mail revert-all dry-run) — sprint #510 |
| #511 | calendar | Touch-overrides bulk dry-run — `?dry=true` no #507 retorna `{dry:true, event_id, touched, not_found}` sem `EventRepo::update`, sem ETag/updated_at/DTSTAMP, sem `EventUpdated`; nova `TouchOverridesBulkQuery` struct + `Query<TouchOverridesBulkQuery>` extractor adicionado ao `touch_overrides_bulk`; ramo dry valida instances igual ao real (parse `parse_one_exdate`, dedup `target_compact`, check `has_recurrence_id_override(&ev.ical_raw, …)`) e particiona em `touched`/`not_found` sem aplicar `patch_recurrence_id_override_block`; mesma validação 400 (lista 1..256, master sem UID) e 404 (touched vazio) que path real; default `dry=false` preserva semantics #507; UI consegue prever "instances X/Y/Z viram tocadas, A/B não existem" antes de rodar; segundo dry-run da família touch (#510 fez touch-all primeiro) — sprint #511 |
| #512 | calendar | Touch-overrides-by-range dry-run — `?dry=true` no #509 retorna `{dry:true, event_id, touched, skipped}` sem `EventRepo::update`, sem ETag/updated_at/DTSTAMP, sem `EventUpdated`; campo `dry: Option<bool>` adicionado a `TouchOverridesByRangeQuery` (composto com `after`/`before`); ramo dry walka `list_recurrence_id_overrides(&ev.ical_raw, …)` igual ao real, parseia compact via `parse_one_exdate`, aplica filtros `[after, before)` half-open, particiona em `touched`/`skipped` sem patch; mesma validação 400 (`after >= before`, master sem UID) e 404 (touched vazio) que path real — UI não vê dry "ok" mas real "fail"; default `dry=false` preserva semantics #509; trio bulk dry-run da família touch fechado (#510 touch-all + #511 bulk-list + #512 by-range); resta apenas dry-run nos singles #505/#506 — sprint #512 |
| #513 | calendar | Touch single dry-run — `?dry=true` no #505 (`POST /overrides/:recurrence_id/touch`) e #506 (`POST /:id/touch`) retorna `{dry:true, event_id, [recurrence_id,] touched:true}` sem `EventRepo::update`, sem ETag/sequence/dtstamp, sem `EventUpdated`; nova struct compartilhada `TouchSingleQuery { dry: Option<bool> }` + `Query<TouchSingleQuery>` extractor adicionado a ambos handlers; ramo dry só roda DEPOIS dos checks reais (assert_can_write, EventRepo::get, parse_one_exdate, extract_uid, has_recurrence_id_override) — preserva 100% das validações 400/404 do path real; default `dry=false` preserva semantics original; **família touch dry-run 100% completa:** 5 endpoints × 5 dry-runs (#510 touch-all + #511 bulk-list + #512 by-range + #513 touch-single + override-single) — sprint #513 |
| #514 | calendar | Touch combined preview — `GET /api/v1/calendars/:cal_id/events/:id/touch-preview?after=&before=` consolida em 1 chamada o que SERIA tocado por `touch-all` (#508) + `touch-overrides-by-range` (#509) sem nenhum side effect; ortogonal aos POST `?dry=true` (#510-#513) que precisam de WRITE+ por serem POST short-circuit; este é GET puro READ-only (não exige `assert_can_write`); retorna `{event_id, master:true, total_overrides, in_range, out_of_range, unparseable}` agregando 3 dimensões num só payload — `master` sempre true (touch-all sempre tocaria), `in_range`/`out_of_range` particiona via filtros half-open `[after, before)` opcionais (mesma semantics #509), `unparseable` lista RECURRENCE-IDs que `parse_one_exdate` rejeita (compact corrompido); sem `after`/`before`, todos vão pra `in_range` (degenera ≡ touch-all sem master); novo `TouchPreviewQuery { after, before }` com `time::serde::rfc3339::option`; reusa `list_recurrence_id_overrides(false)` walker; 400 se `after >= before` ou master sem UID; útil pra UI "discovery" antes de qualquer mutação (audit/dry preview unificado) — sprint #514 |
| #515 | calendar | Touch-preview parseable filters — `?include_unparseable=false` (default `true`) esconde a lista `unparseable` do payload mas mantém no `total_overrides` (UI sabe que existem N items corrompidos sem precisar listá-los); `?only_parseable=true` (default `false`) exclui de TUDO — payload e count refletem só o universo de RECURRENCE-IDs que `touch-all` efetivamente conseguiria mexer; flags compostas: `only_parseable=true` + `include_unparseable=true` explícito → 400 (conflito); `only_parseable=true` implica `include_unparseable=false` automaticamente; campo opcional do JSON omitido em vez de `[]` quando escondido (consistência com pattern de detail-aware response); zero impacto em request sem flags (shape original do #514 100% preservado); útil pra UI "modo limpo" não mostrar lixo + UI "modo análise" agregando count completo sem detalhes — extensão direta do #514 — sprint #515 |
| #516 | calendar | EXDATE list filter por kind — `GET /api/v1/calendars/:cal_id/events/:id/exdates?detail=full&kind=utc\|tzid\|date-only\|unknown` filtra a lista pelo `kind` parseado em `parse_exdates_rich` (extensão direta do #504); novo campo `kind: Option<String>` em `ListExdatesQuery` validado pra um dos 4 valores fixos (qualquer outro → 400); só faz sentido com `detail=full` porque `summary` degenera pra UTC-only (pula TZID/date-only/unknown silenciosamente) — `kind=utc` em `summary` é no-op aceito (sem warning); `kind=tzid\|date-only\|unknown` com `summary` → 400 explícito ("requires detail=full"); `kind` ausente preserva 100% shape do #504 (filter degenera num pass-through); útil pra audit "quais EXDATEs estão em formato MVP-unsupported" (`kind=unknown`) ou "quais precisam migrar pra UTC" (`kind=tzid`); read-only, não requer WRITE; resposta mantém formato `{event_id, count, exdates:[...]}` com `count` refletindo só items que sobraram no filtro — sprint #516 |
| #517 | calendar | Overrides list range filter — `GET /api/v1/calendars/:cal_id/events/:id/overrides?after=&before=` filtra a lista de RECURRENCE-ID overrides por intervalo half-open `[after, before)` parseando o `compact` de cada item via `parse_one_exdate` (paralelo simétrico do `touch-overrides-by-range` #509 e do EXDATE list filter #516); ambos opcionais (RFC3339), ausência total preserva 100% shape do #496/#503; quando algum bound é dado, RECURRENCE-IDs não-parseáveis (TZID-based, etc.) são pulados silenciosamente — sem range, todos aparecem (mesmo formato exótico); composto com `?detail=full` do #503 (filtra primeiro pelo walker, range depois via `retain`); 400 se `after >= before`; read-only, não requer WRITE; `count` reflete só items pós-filtro; útil pra UI "esta semana"/"próximo mês" sem N+1 GETs e sem listar tudo + filtrar client-side — sprint #517 |
| #518 | calendar | Overrides list filter por presença — `GET /api/v1/calendars/:cal_id/events/:id/overrides?has_summary=&has_dtstart=&has_dtend=` filtros booleanos qualitativos em AND aplicados após o range filter do #517; cada flag opcional independente — `true` exige campo presente no override, `false` exige ausência (campo nulo no JSON); combinados segmentam categorias semânticas: "só rename de título" (`has_summary=true&has_dtstart=false&has_dtend=false`) vs. "só reschedule" (`has_summary=false&has_dtstart=true`); aplicados via segundo `Vec::retain` condicional (skip total quando todas 3 são `None`) que checa `item.get(key).map(|v| !v.is_null())` por campo; `description`/`location` ficam de fora porque só estão presentes em `?detail=full` — assimétrico, não vale flag dedicada; ausência total dos 3 flags preserva 100% shape do #517 (e por extensão #496/#503); read-only, não requer WRITE; `count` reflete intersecção range+presença pós-filtro; padrão: filter chains ortogonais (range temporal + presença qualitativa) compostos via `Vec::retain` sequenciais condicionais — variant qualitativa do #517 — sprint #518 |
| #519 | calendar | Overrides count-by-detail stats — `GET /api/v1/calendars/:cal_id/events/:id/overrides/stats` agrega counts de presença dos campos `summary`/`dtstart`/`dtend` em todos os overrides do evento (agregado do filter qualitativo do #518); retorna `{event_id, total, by_field:{summary:{present,absent}, dtstart:{...}, dtend:{...}}, by_category:{none, only_summary, only_dtstart, only_dtend, summary_dtstart, summary_dtend, dtstart_dtend, all_three}}`; `by_field` é cardinality marginal (`present + absent = total` por campo, somas independentes), `by_category` particiona em 8 buckets disjuntos por combinação de presença (soma das 8 categorias = total); útil pra dashboards exibirem distribuição "só rename" vs "só reschedule" vs "rename + reschedule" sem puxar lista inteira do #518 e contar client-side; rota literal `/overrides/stats` precede `/overrides/:recurrence_id` (axum matcha literal antes de param wildcard); description/location ficam de fora pela mesma assimetria do #518 (só existem em `?detail=full`); reusa `list_recurrence_id_overrides(raw, uid, false)` do #503/#517/#518; read-only, não requer WRITE; 404 se evento não existe; complementa o filter do #518 (sample) com agregado completo (population) — sprint #519 |
| #520 | calendar | Overrides stats com filtros range — `?after=&before=` em `GET /api/v1/calendars/:cal_id/events/:id/overrides/stats` (composição de #517 + #519) restringe o agregado a janela temporal half-open `[after, before)`; nova `OverridesStatsQuery { after, before }` com `time::serde::rfc3339::option`, `Query<OverridesStatsQuery>` extractor adicionado ao handler; reusa o `Vec::retain` do #517 (parse `compact` via `parse_one_exdate`, filtros de bounds opcionais, RECURRENCE-IDs não-parseáveis pulados silenciosamente quando algum bound é dado) aplicado ANTES do loop de contagem; `total`/`by_field`/`by_category` agregam só sobre items pós-filtro — invariants preservadas (`present + absent = total` por campo, soma das 8 categorias = total); ambos opcionais — ausência total preserva 100% shape do #519; 400 se `after >= before`; útil pra dashboards "distribuição de overrides nesta semana" sem listar tudo (#518 + range) e agregar client-side — variant range-aware do #519, complementa o trio overrides {list filtered #517+#518, agg #519, agg filtered #520} — sprint #520 |
| #521 | calendar | Overrides stats com filtros presença — `?has_summary=&has_dtstart=&has_dtend=` em `GET /api/v1/calendars/:cal_id/events/:id/overrides/stats` (composição de #518 + #519, paralelo qualitativo do #520); 3 novos campos `has_summary/has_dtstart/has_dtend: Option<bool>` em `OverridesStatsQuery` com `#[serde(default)]` (Option<bool> tem Deserialize natural, sem `with`); novo `Vec::retain` qualitativo aplicado APÓS o range retain do #520 (sequencial — `Vec::retain` chains ortogonais), só roda se pelo menos um dos 3 flags é `Some`; helper local `present = |key| item.get(key).map(|v| !v.is_null()).unwrap_or(false)` compartilhado entre os 3 checks; combinado com `?after=&before=` do #520 permite agregar "entre os que SÓ renomeiam, quantos por janela temporal" (interseção range + presença); `total`/`by_field`/`by_category` agregam só sobre items pós-filtro qualitativo — invariants preservadas igual ao #520; description/location ficam de fora (mesma assimetria #518: só existem em `?detail=full`); ausência total dos 3 flags ≡ shape #520; read-only, NÃO requer WRITE+; padrão consolidado: stats endpoints replicam TODOS os filtros disponíveis no list endpoint correspondente — `?after/before/has_*` do #517+#518 agora todos disponíveis em `/stats`, mantendo `(filter, agg)` como par dual completo — sprint #521 |
| #522 | calendar | EXDATE count-by-kind stats — `GET /api/v1/calendars/:cal_id/events/:id/exdates/stats` agrega counts de EXDATEs por classificação de formato (paralelo simétrico do #519/#520/#521 mas pra EXDATE list com count-by-kind ao invés de count-by-presence); retorna `{event_id, total, by_kind:{utc, tzid, date_only, unknown}}` onde os 4 buckets são DISJUNTOS — soma das 4 = total (invariant testável); reusa `parse_exdates_rich` do #511/#516 sem extensão (mesma fonte de classificação) iterando uma vez com `match info.kind.as_str()` 4-arm exaustivo; rota literal `/exdates/stats` precede `/exdates/:instance` no router (axum matcha literal antes de wildcard); útil pra dashboards "distribuição de formatos de EXDATE" + audit "quantos estão em formato não-MVP" via `by_kind.unknown + by_kind.tzid + by_kind.date_only` sem puxar lista inteira do #511 e classificar client-side; `?after=&before=` (paralelo simétrico do #520) restringe janela temporal half-open `[after, before)` aplicada APÓS o kind retain — só EXDATEs com `parsed_utc=Some` sobrevivem; TZID/date-only/unknown silenciosamente excluídos quando algum bound é dado (consistente com #517/#520); `?kind=utc|tzid|date-only|unknown` (paralelo simétrico do #516 mas em stats) restringe a UM kind via primeiro `Vec::retain`; combinação `kind=tzid|date-only|unknown` + range = sempre `total=0` por design (sem `parsed_utc`); 400 em kind inválido ou `after >= before`; sem nenhum dos 3 flags ≡ shape baseline (todos EXDATEs entram no agregado); read-only, NÃO requer WRITE+; padrão `(filter, agg)` dual completo agora replicado também no EXDATE — kind+range filters do #516+#517 todos disponíveis em `/exdates/stats` — sprint #522 |
| #525 | calendar | EXDATE preview combo — `GET /api/v1/calendars/:cal_id/events/:id/exdates-preview?after=&before=[&include_non_utc=false][&only_utc=true]` (sprint #525, paralelo simétrico do touch-preview #514/#515 mas pra EXDATE list — fechando paralelo "EXDATE catching up com overrides" iniciado em #511); read-only GET puro (NÃO requer WRITE+ — paralelo aos GETs do #496/#503/#504/#514, oposto aos POSTs touch-by-range), reusa `parse_exdates_rich` (mesma fonte do #511/#516/#522/#523/#524) sem extensão; itera uma vez particionando em 3 listas: `in_range` (EXDATEs UTC parseáveis com `parsed_utc=Some` dentro de `[after, before)` half-open), `out_of_range` (UTC parseáveis fora), `non_utc` (kind=tzid\|date-only\|unknown — sem `parsed_utc` pra comparar, classe semantic equivalente ao `unparseable` do touch-preview); 2 flags `include_non_utc/only_utc: Option<bool>` direto do pattern do #515 — `include_non_utc=false` esconde `non_utc` do payload mas mantém em `total_exdates`, `only_utc=true` exclui `non_utc` de TUDO (payload + count) refletindo só o universo "comparável"; conflito `only_utc=true` + `include_non_utc=true` explícito → 400 (mesma regra do #515); dedup defensivo via `iter().any()` por `raw_value` (chave estável — preserva linha original incluindo TZID/params, paralelo ao `compact` do touch-preview); retorna `{event_id, total_exdates, in_range, out_of_range[, non_utc]}`; sem `after`/`before`, todos os UTC vão pra `in_range` (caso degenerado ≡ list-utc-only); 400 se `after >= before`; 404 se evento não existe ou `calendar_id` não bate; complementa o list `/exdates?detail=full` (#504/#516/#523): list retorna metadata rica (raw_value, tzid, params) com filtros categóricos/qualitativos pra drill-down, preview retorna `raw_value`s agrupados em buckets temporais pra dashboard "qual a janela" — endpoints duais semelhantes ao filter #517/#518 vs stats #519-#521 dos overrides; padrão consolidado: `*-preview?after=&before=` é o complemento read-only/discovery dos POSTs touch/bulk com janela temporal — toda família de endpoints com `?after=&before=` deve oferecer preview GET correspondente sem WRITE+ — sprint #525 |
| #524 | calendar | EXDATE stats filter qualitativo — `?with_tzid=true\|false&with_params=true\|false` em `GET /api/v1/calendars/:cal_id/events/:id/exdates/stats` (sprint #524, paralelo simétrico do #523 mas em stats — fechando o trio EXDATE como já fechamos overrides com #519/#520/#521); 2 novos campos `with_tzid/with_params: Option<bool>` em `ExdatesStatsQuery` com `#[serde(default)]`; novo `Vec::retain` qualitativo aplicado APÓS o range retain do #522 (sequencial — chains ortogonais, mesmo pattern do #521 que adicionou retain após #520) só roda se pelo menos um dos 2 flags é `Some`; ortogonalidade `kind=tzid` ⊂ `with_tzid=true` (kind=tzid implica TZID, mas `with_tzid=true` também captura `kind=unknown` com TZID malformado) preservada do #523 — combinações impossíveis (e.g. `kind=utc&with_tzid=true`) aceitas e retornam `total=0` consistente com `kind=tzid` + range do #522; `total`/`by_kind` agregam só sobre items pós-filtro qualitativo — invariant da soma das 4 categorias = total preservada pós-retain; ausência total dos 2 flags ≡ shape #522; read-only, NÃO requer WRITE+; padrão `(filter, agg)` dual COMPLETO agora replicado no EXDATE também — kind/range/with_* todos disponíveis em `/exdates` E `/exdates/stats`, simetria total com overrides (#517-#521 list+stats com range+presença); insight estrutural: stats endpoints sempre replicam TODOS os filtros do list correspondente — toda nova qualquer flag introduzida no list deve ser portada pra stats no sprint imediatamente seguinte (pattern observado: #517→#520, #518→#521, #516+#523→#522+#524) — sprint #524 |
| #527 | calendar | Touch-preview rico — `?detail=full` em `GET /api/v1/calendars/:cal_id/events/:id/touch-preview` engrandece `in_range`/`out_of_range` de `Vec<String>` (só compacts, default do #514) pra `Vec<{compact,summary,dtstart,dtend,description,location}>` permitindo UI confirmar visualmente "vai mexer nestes 12 eventos: 'Reunião X', 'Reunião Y', ..." antes de touch-all (sprint #527, paralelo direto do `?detail=full` do #503 mas em preview agregado); novo campo `detail: Option<String>` em `TouchPreviewQuery` validado contra `"summary"` (default, preserva 100% shape do #514) ou `"full"` (objetos rich), outro → 400; ramificação simples `let full = matches!(q.detail, ..."full"...)` repassada como bool pro `list_recurrence_id_overrides(raw, uid, full)` (mesmo walker do #503 que já suportava o flag — zero extensão do helper, só ativa o branch `description+location`); `in_range`/`out_of_range` viram `Vec<serde_json::Value>` (typed-shape varia mas sempre serializa pra JSON consistente — quando `full=false` empurra `Value::String(compact)` paralelo ao shape pre-#527, quando `full=true` empurra `item.clone()` do walker direto); `unparseable` continua `Vec<String>` mesmo em `full` por definição — RECURRENCE-ID corrompido não tem campos parseáveis (não bate UID/walker rejeitaria) então enriquecimento é meaningless; dedup defensivo refatorado pra `seen: Vec<String>` separado (em vez de `iter().any()` em 3 buckets — economiza N comparações por item, paralelo da otimização aplicada no #526); flags `include_unparseable/only_parseable` do #515 e `after/before` do #514 continuam funcionando idênticas — composição completa `?detail=full&after=&before=&include_unparseable=&only_parseable=` ortogonal; ausência de `detail` ≡ shape #514/#515 100% backward-compat (campo `seen` é interno e não vaza); read-only, NÃO requer WRITE+; padrão consolidado: endpoints preview com items naturalmente coletáveis em modo "rico" (RECURRENCE-IDs aqui, EXDATE rich seria parecido) devem oferecer `?detail=full` opt-in espelhando o list correspondente — UI sample pra dashboard de count usa `summary` (compact-only, payload pequeno), UI confirm dialog usa `full` (objetos com summary/dtstart/dtend pra preview visual humano); insight: `Vec<serde_json::Value>` heterogêneo entre branches `full=true` (objects) e `full=false` (strings) é aceitável quando JSON-shape é a interface — Rust types se ajustam à serialização target — sprint #527 |
| #526 | calendar | EXDATE preview-stats agregado — `GET /:id/exdates-preview/stats?after=&before=[&include_non_utc=][&only_utc=]` (sprint #526, paralelo da dualidade preview/stats — pra preview, do que #519/#520/#521 fizeram pra list de overrides e #522/#523/#524 pra list de EXDATE); reusa `parse_exdates_rich` + mesma partição em 3 buckets do #525 (`in_range`/`out_of_range`/`non_utc`), mas em vez de empurrar `raw_value` pra `Vec<String>` só incrementa contadores `usize` — útil pra dashboard "quantos cancelamentos caem nesta janela" sem puxar lista de strings (pode ser N grande, raw_value carrega TZID/params que ocupam espaço); flags `include_non_utc/only_utc` com semantics IDÊNTICA ao #525 (`include_non_utc=false` omite `non_utc_count` mas mantém em `total_exdates`; `only_utc=true` exclui non_utc de TUDO; conflito explícito `only_utc=true && include_non_utc=true` → 400); reusa o mesmo `ExdatesPreviewQuery` struct do #525 (zero duplicação — query model partilhado entre preview e preview-stats, paralelo do trio `OverridesStatsQuery` reusado em #520/#521); dedup defensivo via `seen: Vec<String>` por `raw_value` (mesma chave estável do #525); rota `/exdates-preview/stats` adicionada APÓS `/exdates-preview` (literal puro, axum sem ambiguidade — paralelo da rota `/overrides/stats` antes de `/overrides/:recurrence_id` do #519); retorna `{event_id, total_exdates, in_range_count, out_of_range_count[, non_utc_count]}`; sem `after`/`before`, todos os UTC vão pra `in_range_count` (degenera ≡ count-utc-only do `parse_exdates_rich`); 400 se `after >= before`; 404 se evento não existe ou `calendar_id` não bate; read-only, NÃO requer WRITE+; padrão consolidado: `*-preview` (raw_value lists) + `*-preview/stats` (counts agregados) é par dual de discovery — toda família com preview deve oferecer stats correspondente, símetrico ao trio list/stats de overrides+EXDATE; insight: query struct REUSADO entre preview e preview-stats reduz drift de validação — flags evoluem no struct uma única vez e ambos handlers herdam — sprint #526 |
| #530 | calendar | EXDATE preview-stats tzid breakdown — `tzid_breakdown: {"Europe/Berlin": N, "America/Sao_Paulo": M, ...}` em `GET /api/v1/calendars/:cal_id/events/:id/exdates-preview/stats` agrega ocorrências de cada TZID DISTINTO entre os EXDATEs com `kind="tzid"` (sprint #530, drill-down adicional do bucket `tzid` do `non_utc_by_kind` do #529 — particiona o count opaco em entradas por timezone); mantém disjunto: só conta items com `kind="tzid"` E `info.tzid=Some` (não inclui `unknown` mesmo com TZID malformado, nem `date-only`) — invariant testável: `sum(tzid_breakdown.values()) == non_utc_by_kind.tzid`; implementação `Vec<(String, usize)>` association list (não `HashMap` — cardinalidade típica é baixa, N TZIDs distintos por evento, lookup linear `iter().position()` mais barato que hash em pequena escala) acumulando dentro do branch `kind="tzid"` adicionado no #529 (zero overhead em items UTC ou outros kinds); serializado como `serde_json::Map` pra suportar chaves dinâmicas (TZID strings) construído via `for (tz, n) in &tzid_breakdown { breakdown_obj.insert(...) }` em vez do macro `json!` (que precisa keys estáticas); ordem de iteração = ordem de aparição no walker (estável pra mesma input mas não ordenado lexicograficamente — UI deve sortear se quer ordem alfabética); shape final `{event_id, total_exdates, in_range_count, out_of_range_count[, non_utc_count, non_utc_by_kind, tzid_breakdown]}` — `tzid_breakdown` segue MESMA regra de omissão do `non_utc_count`/`non_utc_by_kind` (escondido quando `include_non_utc=false` ou `only_utc=true`); objeto vazio `{}` quando `non_utc_by_kind.tzid=0` (não omitido nem `null` — semantically "0 TZIDs distintos" é um agregado válido); reusa `ExdatesPreviewQuery` do #525/#526/#529 sem novo flag — info já tava no walker via `ExdateInfo.tzid`, só precisava expor; flags `include_non_utc/only_utc` + `after/before` continuam idênticas — composição completa preservada; ausência ≡ shape #529 + 1 campo agregado opcional (backward-compat); read-only, NÃO requer WRITE+; útil pra audit "quais timezones aparecem nas EXDATEs não-UTC do calendário" + migration planning "Europe/Berlin tem 47 EXDATEs, vai precisar de N conversions"; padrão consolidado: contadores opacos (`non_utc_count`, depois `non_utc_by_kind.tzid`) sempre admitem drill-down quando o walker carrega dimensão acionável — sequência #526 (count opaco) → #529 (split por kind) → #530 (split por tzid distinto) demonstra o pattern "agg endpoints crescem em granularidade, cada nova flag adiciona drill-down sem invalidar agregados anteriores"; insight: chaves dinâmicas em JSON (TZID arbitrários) requerem `serde_json::Map::insert` no construtor manual em vez do macro `json!` — pattern útil pra qualquer agregação por valor-de-runtime (TZID, user_id, calendar_id etc.) — sprint #530 |
| #529 | calendar | EXDATE preview-stats by-kind — `non_utc_by_kind: {tzid, date_only, unknown}` em `GET /api/v1/calendars/:cal_id/events/:id/exdates-preview/stats` particiona o `non_utc_count` do #526 nos 3 únicos `kind`s que produzem `parsed_utc=None` (sprint #529, paralelo do `by_kind` do #522 mas dentro do bucket "non-UTC" do preview-stats — fechando dualidade `by_kind` em ambos stats endpoints da família EXDATE: #522 list-stats classifica TUDO em 4 categorias incluindo `utc`, #529 preview-stats classifica só os 3 não-UTC dentro do `non_utc_count`); reusa `parse_exdates_rich` (mesma fonte do #525/#526) iterando uma vez com 3 contadores `usize` adicionais (`k_tzid`, `k_date_only`, `k_unknown`) acumulados via `match info.kind` 3-arm exaustivo dentro do branch `parsed_utc=None` (zero overhead em items UTC normais — só conta quando já cai em `non_utc_n += 1`); soma das 3 categorias = `non_utc_count` (invariant testável: `tzid + date_only + unknown == non_utc_count`); shape final `{event_id, total_exdates, in_range_count, out_of_range_count[, non_utc_count, non_utc_by_kind]}` — `non_utc_by_kind` segue a MESMA regra de omissão do `non_utc_count` do #526 (omitido quando `include_non_utc=false` ou `only_utc=true`); chave `date_only` (snake_case) em vez do `kind` literal `"date-only"` (com hífen) pra ficar JSON-key-friendly em consumidores (JS `obj.date_only` é mais natural que `obj["date-only"]`); reusa `ExdatesPreviewQuery` do #525/#526 sem novo flag — info já tava no walker, só precisava expor; flags `include_non_utc/only_utc` + `after/before` continuam idênticas — composição completa preservada; ausência ≡ shape #526 + 1 campo agregado opcional (backward-compat); read-only, NÃO requer WRITE+; útil pra dashboard "quais EXDATEs preciso migrar pra UTC vs quais estão corrompidas" sem puxar lista do #528 e agrupar client-side; padrão consolidado: stats endpoints com bucket "lixo" (non_utc/unparseable) devem oferecer sub-classificação por `kind` quando o walker já produz a classificação — toda agregação `*_count` opaca deve ter contraparte `*_by_kind` quando há dimensão semantic acionável; insight: a partição `(utc, tzid, date-only, unknown)` é INVARIANT semantic do `parse_exdates_rich` — cada endpoint que reusa o walker deve expor essa partição na granularidade apropriada (list filter no #516, list-stats agg no #522, preview-stats agg parcial no #529) — sprint #529 |
| #528 | calendar | EXDATE preview rico — `?detail=full` em `GET /api/v1/calendars/:cal_id/events/:id/exdates-preview` engrandece `in_range`/`out_of_range`/`non_utc` de `Vec<String>` (só `raw_value`s, default do #525) pra `Vec<{compact, rfc3339, kind, raw_value, tzid, params}>` permitindo UI confirmar visualmente "vai cancelar estas 12 instâncias com TZID Europe/Berlin: ..." (sprint #528, paralelo simétrico do #527 mas pra EXDATE — fechando dualidade `?detail=full` em ambos preview endpoints da família calendar); novo campo `detail: Option<String>` em `ExdatesPreviewQuery` validado contra `"summary"` (default, preserva 100% shape do #525) ou `"full"` (objetos rich), outro → 400; reusa `parse_exdates_rich` (mesma fonte do #511/#516/#517/#522/#523/#524/#525/#526) que JÁ produz `ExdateInfo { raw_value, tzid, params, parsed_utc, kind }` exatamente o shape rico desejado — zero extensão de helper, só serializa direto pra `serde_json::json!({...})` no full branch; `in_range`/`out_of_range`/`non_utc` viram `Vec<serde_json::Value>` (typed-shape varia entre full=`object` e summary=`Value::String(raw_value)` — mesmo pattern heterogêneo do #527); shape do item rico ESPELHA o `/exdates?detail=full` do #511/#516/#523 (`compact`, `rfc3339`, `kind`, `raw_value`, `tzid`, `params`) — consistência cross-endpoint: preview e list mostram items idênticos quando enriquecidos, paralelo do mesmo princípio em `touch-preview?detail=full` (#527) reusando shape do walker `list_recurrence_id_overrides`; em `non_utc`, `compact`/`rfc3339` ficam `null` (não há `parsed_utc` pra formatar — kind=tzid/date-only/unknown), MAS o item ainda enrichece com `kind`/`raw_value`/`tzid`/`params` (diferença vs `unparseable` do #527 que permanece `Vec<String>` — RECURRENCE-ID corrompido não tem campos parseáveis, mas EXDATE não-UTC ainda tem metadata rica via `parse_exdates_rich`); dedup defensivo refatorado pra `seen: Vec<String>` separado (consistente com otimização aplicada em #526/#527); flags `include_non_utc/only_utc` do #525 + `after/before` continuam funcionando idênticas — composição `?detail=full&after=&before=&include_non_utc=&only_utc=` ortogonal; ausência de `detail` ≡ shape #525 100% backward-compat; read-only, NÃO requer WRITE+; padrão consolidado: endpoints preview que reusam walkers que JÁ produzem structs ricas (parse_exdates_rich aqui, list_recurrence_id_overrides em #527) devem oferecer `?detail=full` opt-in espelhando o list correspondente — UI dashboard count usa `summary` (raw_value-only, payload pequeno), UI confirm dialog usa `full` (objetos com kind/tzid/params pra preview visual humano); insight: dualidade preview/list rica agora completa pros DOIS lados da família calendar — overrides {list `?detail=full` #503, preview `?detail=full` #527} + EXDATE {list `?detail=full` #511, preview `?detail=full` #528} — toda família futura com walker rico deve oferecer ambas as portas — sprint #528 |
| #531 | calendar | EXDATE list-stats tzid breakdown — `tzid_breakdown: {"Europe/Berlin": N, "America/Sao_Paulo": M, ...}` em `GET /api/v1/calendars/:cal_id/events/:id/exdates/stats` agrega ocorrências de cada TZID DISTINTO entre os EXDATEs com `kind="tzid"` pós-todos-os-filtros (sprint #531, paralelo simétrico do #530 mas no list-stats endpoint #522 — fechando dualidade `tzid_breakdown` em AMBOS stats endpoints da família EXDATE: preview-stats #530 + list-stats #531); diferença crítica vs #530: aqui o accumulator é populado DENTRO do loop de counting já existente (linha pós-retains), portanto é AUTOMATICAMENTE filter-aware — `?kind=`/`?after=&before=`/`?with_tzid=&with_params=` do #522/#524 já aplicaram `Vec::retain` antes, accumulator só vê items pós-filtro; em preview-stats #530 é aplicado durante a partição em buckets (in_range/out_of_range/non_utc) sem filters list-style — ambos approaches matemáticamente equivalentes ao seu contexto: invariant local `sum(tzid_breakdown.values()) == by_kind.tzid` preservada nos DOIS endpoints; implementação `Vec<(String, usize)>` association list IDÊNTICA ao #530 (não `HashMap` — N TZIDs distintos por evento típico baixo, lookup linear `iter().position()` mais barato que hash em pequena escala) acumulando dentro do braço `"tzid"` do `match info.kind` 4-arm já existente — só popula quando item passa nos retains E tem `kind="tzid"` E `info.tzid=Some` (não inclui `unknown` mesmo com TZID malformado, paralelo do #530); serializado via `serde_json::Map::new()` + `.insert()` em vez de macro `json!` pra suportar chaves dinâmicas (TZID strings — runtime values, não literals compile-time); ordem de iteração = ordem de aparição no walker pós-retains (estável pra mesma input mas não lexicograficamente — UI sortear se quer ordem alfabética, paralelo do #530); shape final `{event_id, total, by_kind, tzid_breakdown}` — `tzid_breakdown` SEMPRE presente (diferente do #530 que omite quando `include_non_utc=false`/`only_utc=true` — aqui não há flags equivalentes em ExdatesStatsQuery); objeto vazio `{}` quando `by_kind.tzid=0` ou todos items filtraram-out (não omitido nem `null`); reusa `ExdatesStatsQuery` do #522/#524 sem novo flag — info já tava no walker via `ExdateInfo.tzid`, só precisava expor; filters `kind/after/before/with_tzid/with_params` continuam idênticas — composição completa `?kind=&after=&before=&with_tzid=&with_params=` ortogonal e atua ANTES da agregação `tzid_breakdown` (e.g. `kind=tzid&with_tzid=true&after=...&before=...` agrega TZIDs distintos só dos kind=tzid no range com TZID present, paralelo do "interseção range + presença" do #520+#521); ausência de filters ≡ shape #522 + 1 campo agregado obrigatório (NÃO backward-compat estrito — adiciona campo sempre presente; OK porque consumidores tipicamente ignoram fields desconhecidos); fix paralelo: removido `.as_str()` redundante do `match info.kind` (campo é `&'static str` desde mudança em `parse_exdates_rich`, não `String`); read-only, NÃO requer WRITE+; útil pra audit "quais timezones aparecem nas EXDATEs do calendário todo" (não só preview-window) + migration planning agregada com filters específicos (e.g. "TZIDs nos kind=tzid não-UTC dos últimos 90 dias"); padrão consolidado: dualidade `tzid_breakdown` em preview-stats #530 + list-stats #531 demonstra que aggregates por dimensão dinâmica (TZID, future: user_id, calendar_id) sempre devem ser portados pra AMBOS stats endpoints da família — invariant compartilhada (`sum == by_kind.tzid`) testável cross-endpoint; insight: nova dimensão de drill-down adicionada num stats endpoint deve ser portada pro stats par (preview/list) no sprint imediatamente seguinte — pattern observado: #526→#529 (by-kind), #530→#531 (tzid breakdown), reforçando que stats endpoints devem evoluir EM PAR, não independentemente — sprint #531 |
| #532 | calendar | EXDATE preview-stats top-tzid — `?top_tzid=N` em `GET /api/v1/calendars/:cal_id/events/:id/exdates-preview/stats` trunca o `tzid_breakdown` do #530 pra apenas as N TZIDs mais frequentes (sort by count desc, ties broken por insertion order do `Vec` association list — i.e. ordem de aparição no walker) + adiciona `tzid_other_count: usize` agregando soma das counts das TZIDs descartadas (sprint #532, follow-on do #530 — primeira evolução cosmético/UI sobre o tzid_breakdown; útil pra UI dashboard com ranking-truncado quando há cardinalidade alta de TZIDs >20 distintas, curva tipicamente long-tail com poucas TZIDs frequentes + cauda de ocorrências únicas); novo campo `top_tzid: Option<usize>` em `ExdatesPreviewQuery` com `#[serde(default)]` — flag opcional, ausência (None) preserva 100% shape do #530 (breakdown completo, sem `tzid_other_count`); validação `top_tzid=Some(0)` → 400 ("top_tzid must be >= 1 (use include_non_utc=false to omit breakdown entirely)") porque truncar pra 0 não faz sentido — `include_non_utc=false`/`only_utc=true` já omitem o breakdown inteiro com semantics distinta (omite vs lista vazia); implementação clona `tzid_breakdown: Vec<(String, usize)>` original (preserva insertion order pra branch sem flag), sort_by `b.1.cmp(&a.1)` desc, soma `iter().skip(n).map(|(_, c)| *c).sum()` pra `other_count`, `truncate(n)` no clone — O(N log N) com N=tzids distintos, irrelevante na prática (tipicamente <50); aplicado APÓS o full breakdown ser construído (não dentro do counting loop) — separação clara entre acumulação e apresentação, single-responsibility; no JSON, `tzid_other_count` SÓ aparece quando `top_tzid.is_some()` (mesmo que `tzid_breakdown.len() <= n` resultando em other_count=0 — ainda inclui pra UI saber que a flag foi aceita); compõe naturalmente com `include_non_utc=false`/`only_utc=true` do #525/#529 (que omitem o bloco non_utc inteiro) — top_tzid se torna no-op nesse caso (campo nem chega a ser construído porque `if include_non_utc && !only_utc` envolve toda a serialização); só faz sentido em `/exdates-preview/stats` (não em `/exdates/stats` do #531) porque `ExdatesStatsQuery` é distinto do `ExdatesPreviewQuery` — a dualidade `tzid_breakdown` em preview-stats + list-stats não implica dualidade automática de TODOS os flags subsequentes; top_tzid é cosmético/UI-side independente dos filtros do list-stats (kind/range/with_*), enquanto list-stats já tem filter chain server-side que reduz cardinalidade naturalmente; padrão consolidado: agregados dinâmicos (`tzid_breakdown`) crescem em sofisticação por flag adicional — sequência #530 (breakdown completo) → #532 (truncar + outras) demonstra "agregação primeiro, apresentação depois" — primeiro libera o agregado bruto, depois adiciona flags de presentation (top-N + outros) para casos de UI; insight: pattern "ranking-truncado + bucket residual" (top N + others) é genérico pra qualquer agregação por dimensão dinâmica de cardinalidade alta — futuras agregações (por user_id, calendar_id, etc.) devem seguir mesmo formato `top_X=N` + `X_other_count`; read-only, NÃO requer WRITE+ — sprint #532 |
| #533 | calendar | EXDATE list-stats top-tzid — `?top_tzid=N` em `GET /api/v1/calendars/:cal_id/events/:id/exdates/stats` trunca o `tzid_breakdown` do #531 pra apenas as N TZIDs mais frequentes (sort by count desc, ties broken por insertion order do `Vec` association list pós-retains do #522/#524) + adiciona `tzid_other_count: usize` agregando soma das counts das TZIDs descartadas (sprint #533, paralelo simétrico do #532 mas no list-stats endpoint — fechando dualidade `top_tzid` cross-stats em ambos stats endpoints da família EXDATE: preview-stats #532 + list-stats #533); novo campo `top_tzid: Option<usize>` em `ExdatesStatsQuery` com `#[serde(default)]` — flag opcional, ausência (None) preserva 100% shape do #531 (breakdown completo, sem `tzid_other_count`); validação `top_tzid=Some(0)` → 400 ("top_tzid must be >= 1 (use kind=utc to force tzid_breakdown empty)") — diferente do #532 cuja mensagem aponta `include_non_utc=false`/`only_utc=true` (omite bloco inteiro), aqui a alternativa "agregado sem breakdown" é `?kind=utc` que reduz `by_kind.tzid=0` produzindo `tzid_breakdown = {}` sem omitir o campo (em list-stats `tzid_breakdown` é SEMPRE presente — paralelo do #531); implementação IDÊNTICA ao #532: clona `tzid_breakdown` original (preserva insertion order pra branch sem flag), `sort_by(|a, b| b.1.cmp(&a.1))` desc, soma `iter().skip(n).map(|(_, c)| *c).sum()` pra `other_count`, `truncate(n)` no clone — O(N log N) com N=tzids distintos, irrelevante na prática (<50 típico); aplicado APÓS o full breakdown ser construído pós-counting-loop — separação clara entre acumulação (#531) e apresentação (#533), single-responsibility; no JSON, `tzid_other_count` SÓ aparece quando `top_tzid.is_some()` (mesmo que `breakdown.len() <= n` resultando em other_count=0 — ainda inclui pra UI saber que flag foi aceita, paralelo do #532); diferença filosófica vs #532: aqui o list-stats já tem filter chain server-side (`kind=tzid&after=&before=&with_tzid=&with_params=` do #522/#524) que reduz cardinalidade naturalmente, portanto top_tzid é MENOS crítico — mas oferecido pra consistência cross-endpoint e UI dashboard que pode usar list-stats sem filters quando quer overview agregado total (e.g. dashboard "todas as TZIDs do calendário inteiro" com top-N truncate sem filter por kind/range); composição completa preservada — top_tzid é ortogonal aos filters do #522/#524, atua APÓS a agregação (filter chain reduz o universo do breakdown, top_tzid trunca o breakdown resultante); padrão consolidado: dualidade `top_tzid` cross-stats #532+#533 fecha simetria iniciada em #530+#531 (`tzid_breakdown` cross-stats), demonstrando que TODOS os flags adicionados aos `tzid_breakdown` devem ser portados pra ambos endpoints da família EXDATE (mesmo quando uso é marginal num deles) — pattern "stats endpoints evoluem EM PAR" agora aplica não só ao agregado (#530+#531) mas também aos flags de presentation (#532+#533); insight: pattern "ranking-truncado + bucket residual" (`top_X=N` + `X_other_count`) consolidado como genérico cross-endpoint, futuras agregações por dimensão dinâmica (user_id, calendar_id) devem seguir mesmo formato em TODOS os stats endpoints da família onde o agregado existe — sprint #533 |
| #534 | calendar | EXDATE stats sort_tzid cross-stats — `?sort_tzid=count_desc\|count_asc\|name_asc\|name_desc` em `GET /:id/exdates-preview/stats` E `GET /:id/exdates/stats` ordena o `tzid_breakdown` cross-stats da família EXDATE (sprint #534, presentation flag complementar ao `top_tzid` #532+#533 — paralelo simétrico aplicado aos DOIS endpoints simultaneamente seguindo o pattern "stats endpoints evoluem EM PAR" para flags de presentation consolidado em #532+#533); descoberta técnica crítica: `serde_json::Map` SEM feature `preserve_order` (não ativada no workspace `Cargo.toml`) usa `BTreeMap` interno e serializa Object em ordem alfabética DETERMINÍSTICA — então alterar a ordem de `.insert()` no `breakdown_obj` não afeta o JSON output; única forma de transmitir ordem custom em JSON Object é via array adjacente `tzid_breakdown_order: ["zone_a", "zone_b", ...]` que a UI itera buscando counts no `tzid_breakdown` por chave; novos campos `sort_tzid: Option<String>` em AMBOS `ExdatesPreviewQuery` (paralelo ao `top_tzid` do #532) e `ExdatesStatsQuery` (paralelo ao `top_tzid` do #533) com `#[serde(default)]`; validação match 4-arm contra `count_desc|count_asc|name_asc|name_desc` → `Some(s)`, None ou string vazia → `None` skip, outros → 400 com mensagem listando opções (mesmas em ambos endpoints — semantics IDÊNTICA, sem variação contextual diferente do #532 vs #533 que tinham mensagens de validação distintas); aplicado em DUAS fases compostas com `top_tzid` no `Vec<(String, usize)>` `kept`: (1) selecionar top-N por count desc (semantics original do #532+#533 PRESERVADA); (2) re-ordenar o set retido pelos N elementos via `match sort_mode` 4-arm — `count_desc` → `b.1.cmp(&a.1)`, `count_asc` → `a.1.cmp(&b.1)`, `name_asc` → `a.0.cmp(&b.0)`, `name_desc` → `b.0.cmp(&a.0)`; emite `tzid_breakdown_order: Vec<Value::String>` adjacente ao `tzid_breakdown` SOMENTE quando `sort_mode.is_some()` — flag ausente preserva 100% shape do #533 (sem array adjacente, Object em ordem alfabética default do BTreeMap); composto com `top_tzid=10&sort_tzid=name_asc`: array tem 10 TZIDs em ordem alfabética dos top-10 por count (NÃO os 10 alfabeticamente primeiros do universo total — top-N selection precede sort cosmético); insight não-óbvio: `name_asc` é redundante quando JSON Object já entrega ordem alfabética grátis via BTreeMap, mas torna a ordem EXPLÍCITA (UI não precisa assumir) e permite combinar com `top_tzid` produzindo "top-N alfabeticamente" que NÃO é alcançável sem o pair (sem sort_tzid: top-N por count em ordem alfabética; com sort_tzid=name_asc: top-N por count APRESENTADOS alfabeticamente — mesma seleção, apresentação distinta só observável no array adjacente); `count_desc` é o comportamento default natural pós-truncate do #532+#533 mas torná-lo explícito via flag dá UI chance de gerar `tzid_breakdown_order` mesmo quando `top_tzid` não é dado (ordenar breakdown completo por count); `count_asc` resolve "least-frequent first" útil pra audit "qual o long-tail de TZIDs raras" (filter dual ao top_tzid que mostra heads); read-only, não requer WRITE+; padrão consolidado: limitação técnica (`serde_json::Map` sem preserve_order) gera novo padrão arquitetural — agregados em JSON Object com ordem custom usam array adjacente `*_order: [keys...]` como contrato API estável independente de implementação interna do serializer; pattern aplicável a qualquer agregação por chave dinâmica (TZID, user_id, calendar_id) que precise de ordem custom — sprint #534 |
| #536 | calendar | EXDATE stats `tzid_breakdown_by_kind` cross-stats — `?include_kind_breakdown=true` em AMBOS `GET /:id/exdates-preview/stats` E `GET /:id/exdates/stats` emite o objeto adjacente `tzid_breakdown_by_kind: {tz: {tzid: N, unknown: M}}` particionando o count de cada TZID retido em duas sub-categorias: tokens cujo raw_value parseia como `YYYYMMDDTHHMMSS` local datetime (canônico, contado como `tzid`) vs tokens com TZID presente mas formato local inválido (malformado, contado como `unknown`); diferença filosófica vs `by_kind` do #522 (que classifica TUDO em utc/tzid/date-only/unknown e onde `kind="unknown"` implica TZID ausente): aqui `unknown` é uma SUB-categoria DENTRO de `kind="tzid"` — o item já passou pela validação de TZID-presente em `parse_exdates_rich` mas o token (e.g. `20991301T256000`) não é um datetime local válido; sprint #536, expansão dimensional consolidando o pattern "stats endpoints evoluem EM PAR" agora em 5 dimensões: agregado #530+#531, top_tzid #532+#533, sort_tzid #534, min_count #535, e agora kind-breakdown #536; novos campos `include_kind_breakdown: Option<bool>` em AMBOS `ExdatesPreviewQuery` e `ExdatesStatsQuery` com `#[serde(default)]`; novo helper top-level `is_canonical_local_datetime(&str) -> bool` próximo a `parse_exdates_rich`/`ExdateInfo` usando `time::PrimitiveDateTime::parse` com format_description `[year][month][day]T[hour][minute][second]` (sem Z, sem offset) — escolha deliberada de NÃO alterar `parse_exdates_rich` ou a taxonomia de kinds (que tem ripple em #516/#522/#529 — adicionar nova variante quebraria todos os endpoints que enumeram exhaustivelmente os 4 kinds); validação trivial pelo helper só ativada quando flag presente (zero overhead em items UTC/date-only ou quando flag ausente); novo accumulator `tzid_by_kind: Vec<(String, usize, usize)>` mantido em paralelo a `tzid_breakdown: Vec<(String, usize)>` no MESMO loop por construção — invariant testável `canonical + malformed == tzid_breakdown[tz]` preservada por garantia estrutural (mesma chave, mesmo loop); aplicado APÓS toda a chain de presentation (`min_count` → `top_tzid` → `sort_tzid`) — só os TZIDs no `kept` final aparecem no `tzid_breakdown_by_kind`, na MESMA ordem alfabética default do JSON Object (a ordem custom continua só em `tzid_breakdown_order` do #534 — não duplicada num `*_order_by_kind`); lookup por TZID via `iter().find()` no `tzid_by_kind` durante emissão (linear, mas N pequeno e só executa quando flag set); shape final emitido `{tz: {tzid: N, unknown: M}}` para cada TZID em `kept` — quando TZID não foi encontrado no accumulator (caso raro: kept retido vindo de filtragem mas sem entries — impossível na prática mas blindado com `unwrap_or((0, 0))`); preserva 100% backward-compat: ausência da flag (None ou explicit `false`) NÃO emite o campo; `include_kind_breakdown=true` mas `tzid_breakdown` vazio (e.g. `kind=utc`, `only_utc=true` ou todos filtraram-out) emite `tzid_breakdown_by_kind: {}` (mesma semantics "flag aceita, sem dados" do `tzid_other_count` quando `breakdown.len() <= n` no #533); só faz sentido quando `tzid_breakdown` é incluído (`include_non_utc=true && only_utc=false` no preview-stats; sempre presente no list-stats); composição completa preservada com TODOS os flags anteriores — `?kind=tzid&with_tzid=true&min_count=2&top_tzid=10&sort_tzid=name_asc&include_kind_breakdown=true` é uma chain válida produzindo "top-10 dos TZIDs canônicos+malformados com count>=2 ordenados alfabeticamente, com sub-classificação por canonicidade do token local"; read-only, não requer WRITE+; insight: dimensão `*_by_kind` agora consolidada como QUINTA família de flags ortogonais sobre agregações JSON Object (junto com inclusão #525/#529, truncamento de cabeça #532+#533, filtragem de cauda #535, ordenação cosmética #534) — pattern aplicável a qualquer agregação por chave dinâmica que tenha sub-classificação semantically relevante (TZID canonical/malformed; futuros: user_id active/inactive, calendar_id shared/private); arquitetura de drill-down dimensional sem alteração de taxonomia central (validação leve no presentation handler em vez de novas variantes em parse_exdates_rich) é aplicável a outros endpoints onde adicionar variantes ao walker tem ripple alto — sprint #536 |
| #535 | calendar | EXDATE stats min_count cross-stats — `?min_count=N` em AMBOS `GET /:id/exdates-preview/stats` E `GET /:id/exdates/stats` filtra do `tzid_breakdown` qualquer TZID com `count < N` (sprint #535, dual filosófico ao `top_tzid` #532+#533 — paralelo simétrico aplicado aos DOIS endpoints simultaneamente seguindo o pattern "stats endpoints evoluem EM PAR" consolidado em #534 — agora cobre 4 dimensões: agregado #530+#531, top_tzid #532+#533, sort_tzid #534, min_count #535); semantica: `top_tzid` trunca CARDINALIDADE da CABEÇA (top-N por count desc), `min_count` filtra LONG-TAIL da CAUDA (TZIDs raros) — filosóficamente DUAIS; combinados oferecem janela arbitrária no histograma (`min_count=2&top_tzid=10` = "top-10 entre TZIDs com pelo menos 2 ocorrências cada", recortando ambos extremos do histograma simultaneamente); novos campos `min_count: Option<usize>` em AMBOS `ExdatesPreviewQuery` e `ExdatesStatsQuery` com `#[serde(default)]` (mesmo padrão do `top_tzid`/`sort_tzid`); validação `min_count == Some(0)` → 400 ("min_count must be >= 1 (omit flag for full breakdown)") em ambos endpoints com mensagem IDÊNTICA — semantics não varia contextualmente entre os 2 (mesmo princípio do #534 vs #532+#533 cuja msg de top_tzid=0 variava porque alternativas pra "agregado sem breakdown" diferiam; sort_tzid e min_count têm semantics uniforme cross-endpoint); composição em 3 fases (ordem fixa, escolhida pra preservar semantics intuitiva): (1) `min_count` filtra long-tail removendo TZIDs raros do `Vec<(String, usize)>` original; (2) `top_tzid` seleciona top-N por count desc do UNIVERSO FILTRADO (não do original — composição compostável: `top_tzid=5&min_count=10` = "top-5 entre TZIDs com count>=10", se filtered retém 7 e top_tzid=5 trunca pra 5 com `tzid_other_count`=soma das 2 descartadas pós-filtro); (3) `sort_tzid` ordena o set retido (presentation, do #534); novo campo `tzid_filtered_count: usize` no JSON paralelo do `tzid_other_count` do #532+#533 — mas reportando o que foi excluído pela CAUDA (long-tail filtrada por `min_count`) em vez da CABEÇA truncada (excedente do top-N) — emitido SOMENTE quando `min_count.is_some()` (mesmo que filtered_count=0 — UI sabe que flag foi aceita); `min_count=1` aceito mesmo sendo no-op (todo TZID no breakdown apareceu pelo menos 1x por construção — primeira fronteira "real", UI pode querer `tzid_filtered_count=0` como confirmação de que não há nada na cauda); read-only, não requer WRITE+; insight pattern arquitetural cross-stats consolidado: aggregations em JSON Object por chave dinâmica devem oferecer 4 famílias de flags ortogonais — (a) inclusão/exclusão do bloco inteiro (`include_X`); (b) truncamento da cabeça (`top_X` + `X_other_count`); (c) filtragem da cauda (`min_count` + `X_filtered_count`); (d) ordenação cosmética via array adjacente (`sort_X` + `X_order`) — sprint #535 |
| #550 | calendar | events by-range set-location — `PATCH /api/v1/calendars/:cal_id/events-by-range/set-location?after=&before=&location=` aplica em massa o campo `location` em todos os eventos cujo `dtstart` ∈ `[after, before)` no calendar (sprint #550, quinta mutação da família `events-by-range/*` depois de bulk-move #546, set-status #547, clear-rrule #548 e set-summary #549 — paralelo direto do #549 mas com semantics de "clear" embutida em vez de validação estrita); novo método `EventRepo::set_location_range(tenant, calendar_id, location: Option<&str>, from, to) -> Result<u64>` paralelo direto do `set_summary_range` #549 com UMA diferença crítica de assinatura: `location: Option<&str>` em vez de `&str` — `None` ⇒ `SET location = NULL` (clear), `Some(s)` ⇒ `SET location = $3` (set não-null); SQL idêntico ao #549 (mesma estrutura half-open com `IS NULL OR ...`, single SQL UPDATE em `begin_tenant_tx`, retorna count das linhas afetadas), só troca a coluna `summary` por `location` e o tipo de bind aceita NULL via `Option<&str>` que sqlx serializa naturalmente como SQL NULL; decisão de política não-óbvia: diferente do #549 que rejeitou empty/whitespace `summary` com 400, aqui empty/whitespace OU `?location` ausente é interpretado como CLEAR (NULL) — DDL `location` é nullable e RFC 5545 §3.8.1.7 LOCATION é OPCIONAL em VEVENT (não há `STATUS:`-like obrigatoriedade), portanto "evento sem local" é estado semanticamente válido na ontologia VEVENT (ao contrário de "evento sem summary" que seria UX broken — summary é hint visual primário em todos os calendar UIs); política fundida via filter chain (`q.location.as_deref().filter(|s| !s.trim().is_empty())`) em vez de flag separada `?clear=true` porque a única semantics adicional necessária é "limpar" (não há outras enum-like distinctions tipo "default location" pra introduzir), e cobertura natural pelo valor vazio elimina superfície de API extra; strings não-vazias preservadas como-é após filter (sem trim destrutivo aplicado ao SQL — paralelo direto do #549, UI controla whitespace conforme intenção); limite de tamanho NÃO imposto aqui (DDL TEXT sem CHECK, consistência com `update` regular e #549); mesmo trade-off filosófico do #547/#548/#549 documentado no método repo: `ical_raw` NÃO é re-parseado/reserializado, então a coluna `location` (autoritativa pra GET estruturado da API) fica FRESH, mas o `LOCATION:` dentro do `ical_raw` permanece STALE até próximo PUT/UPDATE do cliente — clientes CalDAV puros parseando o raw verão location antigo (export ICS, download VCAL); novo handler `events_by_range_set_location` + struct `EventsByRangeSetLocationQuery` com `location: Option<String>` (paralelo do `summary: String` do #549 mas wrapped em Option pra suportar omissão = clear); rota `PATCH /api/v1/calendars/:cal_id/events-by-range/set-location` registrada após `events-by-range/set-summary` (#549) na chain do `Router` — sub-path com `/set-location` consolidando convenção "família `events-by-range/*` com sub-rotas verb-by-operation"; single calendar (1 `assert_can_write`); validação `after >= before` → 400 (paralelo universal #509/#517/#522/#537/#543/#544/#545/#546/#547/#548/#549); shape JSON retorno `{calendar_id, location, updated}` com `location: Option<String>` (null no JSON quando clear) — `updated: u64` count das linhas afetadas inclui eventos que já tinham o MESMO location (paralelo do #547/#548/#549, simetria com a família mantida); search FTS `#461` impacto MENOR vs #549 (location costuma ter peso menor que summary no scoring tantivy típico, dependente de schema do indexer — mas index continua stale até reindex, mesmo trade-off "metadata fresh, derived stale" do #549); padrão consolidado: família `events-by-range/*` agora em 7 endpoints — discovery (#544 list + #545 stats) + mutation (#546 move + #547 set-status + #548 clear-rrule + #549 set-summary + #550 set-location), com TEXT-livre nullable representando subcategoria distinta da TEXT-livre obrigatória (#549) e da TEXT-enum obrigatória (#547); insight #1: campos opcionais na DDL (nullable) recebem semantics "empty=clear" naturalmente sem flag adicional, enquanto campos NOT-NULL ou semanticamente-obrigatórios (mesmo que DDL nullable) merecem rejeição de empty — `location` (nullable, opcional) ≠ `summary` (DDL nullable mas UX-obrigatório) explicita que política de empty deve refletir a OBRIGATORIEDADE SEMÂNTICA do campo, não apenas DDL nullability; insight #2: `Option<&str>` em parâmetros repo é o tipo natural pra mutations com semantics "set-or-clear" — sqlx serializa Option::None como SQL NULL automaticamente, então o `Option` do handler propaga até o SQL sem branching adicional (sem necessidade de SQL distinto pra "clear" vs "set"); template "set-{column}" agora cobre 3 modos: TEXT-livre obrigatória (#549 summary), TEXT-livre opcional/clearable (#550 location), TEXT-enum (#547 status); próximas variantes mais simples: `set-description` paralelo direto do #550 (description também é nullable per RFC 5545 §3.8.1.5); `set-organizer-email` ortogonal mas similar (mesmo trade-off); `set-rrule` com validação (deferred — exige RRULE grammar parser) — sprint #550 |
| #549 | calendar | events by-range set-summary — `PATCH /api/v1/calendars/:cal_id/events-by-range/set-summary?after=&before=&summary=` aplica em massa o campo `summary` em todos eventos cujo `dtstart` ∈ `[after, before)` no calendar (sprint #549, quarta mutação da família `events-by-range/*` depois do bulk-move #546, set-status #547 e clear-rrule #548 — primeira mutação de campo TEXT-livre na família, paralelo direto do #547 mas sem enum-validação porque summary é texto-livre per RFC 5545 §3.8.1.12); novo método `EventRepo::set_summary_range(tenant, calendar_id, summary: &str, from, to) -> Result<u64>` paralelo direto do `set_status_range` #547 (mesma estrutura half-open com `IS NULL OR ...`, single SQL UPDATE numa transação `begin_tenant_tx`, retorna count das linhas afetadas — apenas troca a coluna `status` por `summary`); validação no handler: `summary.trim().is_empty()` → 400 ("summary must not be empty") — bulk-set pra string vazia é provavelmente erro de UI; aceitar NULL/vazio explícito via flag `?clear=true` deferred pra sprint futuro se demanda surgir; string preservada como-é após validação (sem trim destrutivo aplicado ao SQL — paralelo do `update` regular em event.rs que também passa valor cru, UI controla whitespace conforme intenção, usuário que quer `"  Reunião  "` literal não é castigado); limite de tamanho NÃO imposto aqui (DDL `summary` é TEXT sem CHECK constraint; consistência com `update` regular que também não tem limit explícito — Postgres aceita até 1GB em TEXT, validação prática fica em VCALENDAR PUT layer pra clientes CalDAV); mesmo trade-off filosófico EXPLÍCITO do #547/#548 documentado no método repo: `ical_raw` NÃO é re-parseado/reserializado, então a coluna `summary` (autoritativa pra GET estruturado da API e search index full-text via #461 events-search-fts) fica FRESH, mas o `SUMMARY:` dentro do `ical_raw` permanece STALE até próximo PUT/UPDATE do cliente — clientes CalDAV puros parseando o raw verão summary antigo (export ICS, download VCAL); particularmente importante porque `summary` aparece em MUITOS lugares: search results (#461 FTS index re-construído incrementalmente em outros endpoints, mas aqui não toca search), iTIP messages (#491 itip-request usa o raw, vê summary antigo), notifications, calendar list views agregadas; UI deve sempre confiar na API estruturada quando disponível, fallback no raw só pra import/export e CalDAV passthrough; decisão paralela ao #547/#548 e justificada pelo MESMO motivo: re-parsear N raws é O(N) string work proibitivo em massa equivalente a N `EventRepo::update`; `etag`/`sequence`/`updated_at` PRESERVADOS pelo mesmo motivo do #546/#547/#548 (mudança no metadata indexado, não no conteúdo iCalendar bruto) — UI/clients que cacheiam por ETag NÃO precisam invalidar; novo handler `events_by_range_set_summary` + struct `EventsByRangeSetSummaryQuery` com `summary: String` obrigatório (não optional, paralelo do `status: String` do #547); rota `PATCH /api/v1/calendars/:cal_id/events-by-range/set-summary` registrada após `events-by-range/clear-rrule` (#548) na chain do `Router` — sub-path com `/set-summary` consolidando convenção "família `events-by-range/*` com sub-rotas verb-by-operation" (paralelo arquitetural do `/move` #546, `/set-status` #547, `/clear-rrule` #548); single calendar (1 `assert_can_write`); validação `after >= before` → 400 (paralelo universal #509/#517/#522/#537/#543/#544/#545/#546/#547/#548); SQL com `($4::timestamptz IS NULL OR dtstart >= $4)` permite ambos bounds opcionais; sem publicação per-event de `EventUpdated` (mesma justificativa do #546-#548: massa); sem `?dry=true` (foundations #544/#545 cobrem discovery); shape JSON retorno `{calendar_id, summary, updated}` paralelo direto do `{calendar_id, status, updated}` do #547 — `updated: u64` count das linhas afetadas inclui eventos que já tinham o MESMO summary (paralelo do #547/#548, simetria com a família — coerência vence reporting preciso, UI compara count com `events-by-range` lista pré-chamada se quer delta exato); IMPORTANTE: search index full-text via `#461 events-search-fts` provavelmente fica STALE pra os summaries atualizados em massa até próxima reindexação (esse endpoint não tem hook pra invalidar tantivy index — futuro: `set-summary` pode disparar reindex incremental, ou agendar reindex full após bulk operations; consistente com pattern "metadata fresh, derived stale" mas afeta UX de search não-trivialmente); padrão consolidado: família `events-by-range/*` agora em 6 endpoints — discovery (#544 list + #545 stats) + mutation (#546 move + #547 set-status + #548 clear-rrule + #549 set-summary), demonstrando que mutações single-field de TEXT-livre (summary, location, description) são mais simples de adicionar que mutações de TEXT-com-validação (status enum, rrule grammar) ou de FK (calendar_id em move); template "set-{column}" agora estabelecido pra próximas variantes (set-location, set-description, set-organizer-email — todas paralelo do #549 só trocando coluna e regra de não-vazio); insight #1: TEXT-livre é o caso mais simples na taxonomia de bulk-set, depois TEXT-enum (#547), depois TEXT-com-grammar (#549+ set-rrule), depois multi-field structural (delete/move por range — não single-field); insight #2: ripple effects de bulk-set em derived data (search index, iTIP outbound, notifications) é dimension não considerada nas mutações anteriores (#547 status enum não rebuilda search; #548 clear-rrule não afeta full-text — só recurrence stats; #549 É O PRIMEIRO que afeta search FTS) — sprintf futuros devem incluir esse mapping consciente: cada column tem set de "consumers downstream" e bulk-set deve documentar quais ficam stale — sprint #549 |
| #548 | calendar | events by-range clear-rrule — `PATCH /api/v1/calendars/:cal_id/events-by-range/clear-rrule?after=&before=` zera (`rrule = NULL`) em massa em todos eventos cujo `dtstart` ∈ `[after, before)` no calendar (sprint #548, terceira mutação da família `events-by-range/*` depois do bulk-move #546 e set-status #547 — variante mais simples do `set-rrule` futuro porque "limpar" é unário sem validação de valor, cabe vir antes; `set-rrule` virá em #549+ e terá que validar a string RRULE antes do UPDATE OU adotar mesmo trade-off do #547/#548 "coluna fresh, raw stale"); novo método `EventRepo::clear_rrule_range(tenant, calendar_id, from: Option<OffsetDateTime>, to: Option<OffsetDateTime>) -> Result<u64>` paralelo direto do `set_status_range` #547 mas SEM parâmetro `value` (só seta NULL hard-coded no SQL — não há ambiguidade nem validação possível); mesmo trade-off filosófico EXPLÍCITO do #547 documentado no método repo: `ical_raw` NÃO é re-parseado/reserializado, então a coluna `rrule` (autoritativa pra GET estruturado da API e queries SQL como `events-recurrence-stats` #464 que conta `rrule IS NULL`, `events-recurrence-monthly` #469 etc) fica FRESH, mas a propriedade `RRULE:` dentro do `ical_raw` permanece STALE até próximo PUT/UPDATE do cliente — clientes CalDAV puros parseando o raw verão o evento como AINDA recorrente (com a antiga RRULE intacta) até reescrita; consequência prática: split brain de view — `events-recurrence-stats` (que conta `WHERE rrule IS NULL`) já reflete o clear imediatamente, mas `export.ics` / download VCAL vê RRULE antiga; UI deve confiar no campo da API estruturada quando disponível, fallback ao raw só pra import/export; decisão paralela ao #547 e justificada pelo MESMO motivo: re-parsear N raws é O(N) string work proibitivo em massa equivalente a N `EventRepo::update`; `etag`/`sequence`/`updated_at` PRESERVADOS pelo mesmo motivo do #546/#547 (mudança no metadata indexado, não no conteúdo iCalendar bruto); novo handler `events_by_range_clear_rrule` + struct `EventsByRangeClearRruleQuery` com APENAS `after`/`before` (zero query params requeridos — paralelo do `delete_range` #457 que também não tem param required, todo opcional); rota `PATCH /api/v1/calendars/:cal_id/events-by-range/clear-rrule` registrada após `events-by-range/set-status` (#547) na chain do `Router` — sub-path com `/clear-rrule` consolidando convenção "família `events-by-range/*` com sub-rotas verb-by-operation" (paralelo arquitetural do `/move` #546, `/set-status` #547); single calendar (1 `assert_can_write` em `cal_id`); validação `after >= before` → 400 (paralelo universal #509/#517/#522/#537/#543/#544/#545/#546/#547); SQL com `($3::timestamptz IS NULL OR dtstart >= $3)` permite ambos bounds opcionais (sem after = sem lower; sem before = sem upper; sem nenhum = limpa rrule de TODOS eventos do calendar com `dtstart` definido — operação total) — paralelo direto do #546/#547; sem publicação per-event de `EventUpdated` (mesma justificativa do #546/#547: massa); sem `?dry=true` (foundations #544/#545 cobrem discovery — UI pode usar `events-by-range?with_rrule=true` pra ver lista exata pré-clear, ou `events-by-range/stats` pra ver `by_recurrence.recurring` count); shape JSON retorno `{calendar_id, cleared}` com `cleared: u64` count das linhas afetadas — minimalista (paralelo do `{deleted}` do #457, `{src,dst,moved}` do #546, `{calendar_id,status,updated}` do #547); IMPORTANTE: count INCLUI eventos que já tinham `rrule = NULL` antes (UPDATE não filtra por valor prévio com `IS DISTINCT FROM` — diferente do `update` em event.rs:188 que tem o guard pra não bumpar etag indevidamente; aqui não bumpa etag de qualquer forma, então não precisa filtrar); `cleared` portanto é "linhas afetadas pelo critério de range no calendar", não "linhas que passaram de RRULE pra NULL"; UI que quer delta exato compara com `by_recurrence.recurring` do `events-by-range/stats` #545 ANTES de chamar (delta = diferença entre o count pré-clear e o `cleared` retornado); decisão de design não-óbvia: `clear-rrule` poderia ter sido `?with_rrule=true` filter no SQL pra reportar count "real", mas violaria simetria com `set-status` #547 (que não filtra por status prévio) e com `move` #546 (que não filtra por dst prévio) — coerência da família vence reporting preciso; padrão consolidado: família `events-by-range/*` agora em 5 endpoints — discovery (#544 list + #545 stats) + mutation (#546 move + #547 set-status + #548 clear-rrule), com `set-rrule` (com validação) cabendo num futuro próximo (#549+) ou outras mutações single-field (set-summary, set-location, set-description) seguindo MESMO pattern; insight: `clear` como caso especial de `set` (com value=NULL) merece endpoint próprio quando o `set` exige validação não trivial — `clear-rrule` veio antes de `set-rrule` justamente porque a validação RRULE é o trabalho real do `set-rrule`, e esse "menor caminho" libera UI já hoje pra cancelar recorrência em massa sem esperar a validação ficar pronta — sprint #548 |
| #547 | calendar | events by-range set-status — `PATCH /api/v1/calendars/:cal_id/events-by-range/set-status?after=&before=&status=` muda em massa o campo `status` em todos os eventos cujo `dtstart` ∈ `[after, before)` no calendar (sprint #547, segunda mutação da família `events-by-range/*` depois do bulk-move #546 — paralelo direto do #546 mas single-tenant single-calendar mexendo em coluna `status` em vez de `calendar_id`); novo método `EventRepo::set_status_range(tenant, calendar_id, status: &str, from: Option<OffsetDateTime>, to: Option<OffsetDateTime>) -> Result<u64>` paralelo direto do `move_range` #546 (mesma estrutura half-open com `IS NULL OR ...`, single SQL UPDATE numa transação `begin_tenant_tx`, retorna count das linhas afetadas); aceita os 3 valores canônicos CalDAV per RFC 5545 §3.8.1.11: `CONFIRMED`, `TENTATIVE`, `CANCELLED` — qualquer outro → 400 com mensagem listando opções; validação case-sensitive UPPER (paralelo do bucket de status do #545 que UPPER-normaliza no SQL — aqui exigimos input já normalizado pra evitar surpresa de "tentative" virar "TENTATIVE" silenciosamente; UI deve emitir UPPER no payload); trade-off filosófico EXPLÍCITO documentado no método repo: `ical_raw` NÃO é re-parseado/reserializado mesmo com STATUS sendo propriedade VCALENDAR válida — re-parsear N raws + reserializar seria O(N) string work no loop equivalente a N `EventRepo::update`; consequência: a coluna `status` (autoritativa pra GET estruturado da API) fica fresh, mas o `STATUS:` dentro do `ical_raw` (visto via export ICS / download VCAL via CalDAV) fica STALE até próximo PUT/UPDATE do cliente; clientes que leem via API JSON (GET /events) verão valor novo imediatamente, clientes CalDAV puros que parseiam `ical_raw` verão valor antigo (clients devem confiar no campo da API estruturada quando disponível, fallback ao raw só pra import/export); decisão similar à do #546 que também não toca `ical_raw` (lá calendar_id é metadata externa ao VCALENDAR — RFC 5545 não tem propriedade calendar-id; aqui status É propriedade VCALENDAR mas custo de re-parse é proibitivo em massa); `etag`/`sequence`/`updated_at` PRESERVADOS pelo mesmo motivo do #546 (mudança no metadata indexado, não no conteúdo iCalendar bruto) — UI/clients que cacheiam por ETag NÃO precisam invalidar; novo handler `events_by_range_set_status` + struct `EventsByRangeSetStatusQuery` com `status: String` obrigatório (não optional) — query string paralelo ao `dst: Uuid` do #546; rota `PATCH /api/v1/calendars/:cal_id/events-by-range/set-status` registrada após `events-by-range/move` (#546) na chain do `Router` — sub-path com `/set-status` consolidando o agrupamento "família `events-by-range/*` com sub-rotas por operação" (paralelo arquitetural do `/move` #546 e da família `overrides-by-range/*` com `/preview` #543); single calendar (não dual como #546) → 1 `assert_can_write` apenas (escopado ao `cal_id` da path); validação `after >= before` → 400 (paralelo universal #509/#517/#522/#537/#543/#544/#545/#546); SQL com `($4::timestamptz IS NULL OR dtstart >= $4)` permite ambos bounds opcionais (sem after = sem lower; sem before = sem upper; sem nenhum = aplica em TODOS os eventos do calendar com `dtstart` definido — paralelo direto do #546); sem publicação per-event de `EventUpdated` (mesma justificativa do #546: massa não cabe no shape per-event do channel events); sem `?dry=true` (foundations #544/#545 cobrem discovery — UI usa `events-by-range/stats?after=X&before=Y` e olha `by_status` pra prever distribuição de status pré-mutação); shape JSON retorno `{calendar_id, status, updated}` com `updated: u64` count das linhas afetadas — minimalista (paralelo do `{src, dst, moved}` do #546 e `{deleted}` do #457); read-only de view do CalDAV: stale `STATUS:` no `ical_raw` é trade-off conhecido (futuro: pode adicionar flag `?rewrite_raw=true` que dispara re-parse+reserialização per-event no loop, custosa mas opcional — UI escolhe entre rapidez vs consistência cross-channel); padrão consolidado: família `events-by-range/*` agora tem 4 endpoints — discovery (#544 list + #545 stats) + mutation (#546 move + #547 set-status), com #548+ podendo adicionar bulk-set-rrule (mais complexo, exige re-parsear `ical_raw` no loop OU mesmo trade-off "rrule autoritativo na coluna, raw stale"); insight: trade-off "metadata indexado fresh, ical_raw stale" é replicável a QUALQUER mutação single-field em massa (status, summary, location, description, organizer_email, rrule) — bulk operations devem priorizar throughput em massa sacrificando consistência cross-channel temporariamente (até próximo PUT do cliente que reescreve o raw inteiro via `replace_by_uid`); separação de operações em paths distintos (`/move`, `/set-status`, futuro `/set-rrule`) confirmada como pattern arquitetural da família — endpoints específicos por operação são mais legíveis, type-safer e facilitam permission gating distinto (move precisa WRITE+ em 2 calendars, set-status só 1) — sprint #547 |
| #546 | calendar | events by-range move — `PATCH /api/v1/calendars/:cal_id/events-by-range/move?after=&before=&dst=` move em massa todos eventos cujo `dtstart` ∈ `[after, before)` no calendar de origem (`cal_id`) pra o calendar de destino (`dst`, mesmo tenant) (sprint #546, primeira mutação real da família `events-by-range/*` depois das foundations read-only #544 list e #545 stats; complementa o trio com `events-bulk-delete` #457 que opera no MESMO universo half-open mas destrutivo, agora 4 endpoints na família `events-by-range/*` + bulk-delete usam critério IDÊNTICO `dtstart ∈ [after, before)` strict half-open); novo método `EventRepo::move_range(tenant, src, dst, from: Option<OffsetDateTime>, to: Option<OffsetDateTime>) -> Result<u64>` paralelo direto do `delete_range` #457 (mesma estrutura da query, single SQL, retorna count das linhas afetadas) — single UPDATE muda APENAS `calendar_id`, sem tocar em `etag`/`sequence`/`updated_at`/`ical_raw`; decisão filosófica chave: `etag` PRESERVADO porque `calendar_id` é metadata CalDAV externa ao VCALENDAR (RFC 5545 não tem propriedade calendar-id; CalDAV usa o path do collection pra associar), então semantically não houve mudança no conteúdo iCalendar (UID/SUMMARY/DTSTART/RRULE idênticos pré/pós-move) — UI/clients que cacheiam por ETag NÃO precisam invalidar (consistência com `move single` e `move bulk` do drive #345/#346 que também preservam metadata de conteúdo); `sequence` PRESERVADO pelo mesmo motivo (RFC 5545 SEQUENCE incrementa em "edição significativa do evento", mover entre calendars não qualifica — paralelo do `update` em event.rs #167 que usa `IS DISTINCT FROM` pra bumpar sequence só quando campos relevantes mudaram); novo handler `events_by_range_move` + struct `EventsByRangeMoveQuery` com `dst: Uuid` obrigatório (não optional) — query string em PATCH em vez de body porque single field, body-less é mais ergonômico via curl/UI (paralelo do `events-bulk-delete` #457 que também usa query em vez de body — convenção da família "by-range" é tudo em query); rota `PATCH /api/v1/calendars/:cal_id/events-by-range/move` registrada após `events-by-range/stats` (#545) na chain do `Router` — sub-path com `/move` consolidando o agrupamento "família `events-by-range/*` com sub-rotas por operação" (paralelo arquitetural do que já temos em `overrides-by-range/preview` #543); requer WRITE+ em AMBOS calendars (origem `cal_id` E destino `dst`) — duplo `assert_can_write` pra cada lado, gate idêntico ao usado em outros endpoints WRITE+ dual-calendar; `dst == cal_id` no-op silencioso (UPDATE match'a 0 linhas porque `WHERE calendar_id = $src` exclui o destino quando coincidem) — semantics "mover pro mesmo lugar não fez nada" é honesta e evita validação extra (paralelo do drive #345 que também aceita destino == origem); validação `after >= before` → 400 (paralelo universal #509/#517/#522/#537/#543/#544/#545); SQL com `($4::timestamptz IS NULL OR dtstart >= $4)` permite ambos bounds opcionais (sem after = sem lower; sem before = sem upper; sem nenhum = move TUDO no calendar de origem com `dtstart` definido — operação total) — paralelo do pattern do `events_recurrence_monthly` #469 que também aceita ambos opcionais; conflito de UID no destino (`(calendar_id, uid)` UNIQUE constraint) → 409 via mapeamento existente do `unique_violation` no error layer (`error.rs:60`) — UI pode escolher "merge" ou "skip" se conflitar; conflito é ATÔMICO (single UPDATE numa transação `BEGIN`/`COMMIT` via `begin_tenant_tx` #1) — se UM evento conflita, TODOS são revertidos (não há move parcial); sem publicação de `EventUpdated` por evento — operação em massa não cabe no shape per-event do channel events; consumers que querem detectar movimentação devem assistir métrica/log do endpoint ou re-listar o calendar (futuro: pode emitir um `BulkEventsMoved` se a demanda surgir, mas mantém narrow agora); `?dry=true` NÃO suportado neste sprint inicial — usuário usa `events-by-range` #544 (lista) ou `/stats` #545 (agregados) pra dry-discovery antes de rodar (foundations #544+#545 já cumprem esse papel — não há razão pra duplicar a lógica de discovery dentro do endpoint mutativo, paralelo da decisão de não duplicar `?dry=true` em `events-bulk-delete` #457 que também não tem flag dry); shape JSON retorno `{src, dst, moved}` com `moved: u64` count das linhas afetadas — minimalista (paralelo do `events-bulk-delete` #457 que retorna `{deleted: u64}`); read-only de view do CalDAV: o ical_raw em si não muda (move é só re-categorização do collection), então o evento no destino tem MESMO UID/ETag/conteúdo do que tinha na origem — clientes CalDAV detectam o move via DAV:sync-collection (mudou a localização do recurso no path-tree), não via mudança no recurso em si; padrão consolidado: família `events-by-range/*` agora completa em 3 dimensões — discovery (#544 lista + #545 stats) + mutation (move agora), com #547+ podendo adicionar bulk-set-rrule (mexe em `rrule` + precisa re-parseamento de cada `ical_raw`, mais complexo) ou bulk-set-status (mais simples, single field text update); insight: bulk-update por range NÃO precisa virar UM endpoint genérico aceitando body com diff — endpoints SEPARADOS por operação (move/set-rrule/set-status) são mais legíveis, type-safer e facilitam permission gating distinto (move precisa WRITE+ em 2 calendars, set-rrule só 1); separação de operações em paths distintos é o pattern arquitetural da família — sprint #546 |
| #545 | calendar | events by-range stats — `GET /api/v1/calendars/:cal_id/events-by-range/stats?after=&before=` retorna `{calendar_id, total, with_rrule, without_rrule, with_dtend, without_dtend, by_status: {CONFIRMED, TENTATIVE, CANCELLED, OTHER}}` agregando contadores sobre o universo `dtstart ∈ [after, before)` no calendário (sprint #545, dual stats do `events-by-range` #544 — paralelo filosófico do `events-recurrence-stats` #464 mas escopado ao critério temporal half-open do #544/#457); diferença chave vs `events-by-range` #544: aqui não há `limit` nem retorno de lista — agrega no SQL via `COUNT FILTER` numa única round-trip, escalável pra calendários gigantes onde listar 10k eventos seria caro mas stats agregadas custam ~ms; total exclui eventos sem `dtstart` (mesmo universo do #544 list, NÃO universo do `events-recurrence-stats` #464 que agrega TUDO incluindo sem-dtstart — divergência intencional pra preservar consistência de universo dentro da família `events-by-range/*`); novo handler `events_by_range_stats` + struct `EventsByRangeStatsQuery` (campos `after`/`before`, ambos opcionais, `time::serde::rfc3339::option`); rota `GET /api/v1/calendars/:cal_id/events-by-range/stats` registrada após `events-by-range` na chain do `Router` — sub-path com `/stats` em vez de path completamente novo (e.g. `/events-by-range-stats`) preservando o agrupamento "tudo do `events-by-range` em uma família de paths" (paralelo do pattern `events/:id/exdates/stats`, `events/:id/overrides/stats`); validação `after >= before` → 400 ("after must be < before") — paralelo do contract universal (#509/#517/#522/#537/#543/#544); SQL: 1 query principal `COUNT FILTER` por 5 buckets (`total`, `with_rrule`, `without_rrule`, `with_dtend`, `without_dtend`) + 1 query secundária `GROUP BY UPPER(COALESCE(NULLIF(status, ''), 'OTHER'))` pra `by_status` — duas round-trips separadas porque `by_status` precisa de cardinalidade dinâmica (não é hardcoded), enquanto os 5 buckets fixos cabem num único `SELECT` (paralelo direto do split entre query principal vs `freq_rows` no #464); `by_status` post-process com match 4-arm em Rust: `CONFIRMED|TENTATIVE|CANCELLED` mantém a chave, qualquer outro coalesce em `OTHER` (acumulando counts via `entry().or_insert + as_i64+ json!(prev+c)` — necessário porque múltiplas linhas distintas no SQL podem cair em `OTHER` e somar); `NULLIF(status, '')` antes de `COALESCE` garante que string vazia (`""`) é tratada como NULL e cai em `OTHER` (RFC 5545 não exige STATUS, mas eventos podem ter o campo presente e vazio); read-only, NÃO requer WRITE+; shape JSON com campos lineares `{calendar_id, total, with_rrule, without_rrule, with_dtend, without_dtend, by_status}` — invariants `with_rrule + without_rrule == total` e `with_dtend + without_dtend == total` preservadas por construção (gates `dtstart IS NOT NULL` em todos os FILTER); `by_status: sum(values) <= total` (nem todo evento com dtstart tem status — diferença vai pra `OTHER`); composição preservada com #544: `events-by-range?after=X&before=Y` (lista) + `events-by-range/stats?after=X&before=Y` (agregados) usam IDÊNTICO universo, UI escolhe entre detail (paginado, ~10k cap) ou agregado (sem cap, escalável); pattern "family path com sub-stats" agora consolidado: cada *-by-range que retorna lista deve ter sub-route `/stats` agregando o MESMO universo — replicável em qualquer endpoint future de discovery por range; insight: separação detail-vs-aggregate é ortogonal à mutação — antes do PATCH em massa do candidato #1 (que precisa de WRITE+ e novos métodos repo), oferecer o stats é continuação natural read-only que enriquece o discovery sem custo adicional na infra; ordem candidate revisada: PATCH em massa real (#546+) agora se beneficia do stats pra UI prever "vai afetar 47 confirmed + 12 cancelled, ok?" antes de rodar — sprint #545 |
| #544 | calendar | events by-range preview — `GET /api/v1/calendars/:cal_id/events-by-range?after=&before=&limit=` retorna `{calendar_id, count, events: [{id, uid, summary, dtstart, dtend, rrule}]}` listando eventos cujo `dtstart` ∈ `[after, before)` no calendário (sprint #544, paralelo direto do `events-bulk-delete` #457 mas em modo discovery sem side effect — foundation pra futuros bulk-update / bulk-set-rrule / bulk-move-calendar); diferença filosófica vs `EventRepo::list` semântica overlap (`dtend >= from AND dtstart <= to` que matches qualquer evento que cruza a janela): aqui o critério é STRICT half-open `dtstart ∈ [after, before)` IDÊNTICO ao `events-bulk-delete` #457 (eventos sem `dtstart` são EXCLUÍDOS — sem critério temporal pra incluí-los, mesma decisão filosófica do bulk-delete); implementação reusa `EventRepo::list` (que já é overlap-aware) e re-filtra strict half-open num segundo passe no handler — necessário porque `EventQuery` não tem variante "strict by dtstart", e adicionar nova método/variante teria ripple em outros call sites do `list`; double-pass é O(N) trivial pós-fetch (limit clamp 1..=10_000 no SQL antes do refilter, então worst-case é re-iterar 10k items que já vieram filtrados pelo overlap mais largo — universo é estritamente subset do retornado pela query, então o limit do SQL pode em tese cortar items que passariam no half-open strict; aceito como trade-off pra reusar repo sem refactor — UI percebe `count == limit` como sinal de truncamento e ajusta janela); novo handler `events_by_range_preview` + struct `EventsByRangePreviewQuery` (campos `after`/`before`/`limit`, todos opcionais, `after`/`before` com `time::serde::rfc3339::option`, `limit: Option<i64>` repassado pra `EventQuery`); rota `GET /api/v1/calendars/:cal_id/events-by-range` registrada após `events-instances` na chain do Router (path com hífen evita colisão com `events/:id` lição #427 — convenção dos `events-bulk-delete` #457, `events-recurrence-stats` #464, `events-recurrence-monthly` #469, `events-instances` #488); validação `after >= before` → 400 ("after must be < before") — paralelo do contract estabelecido em #509/#517/#522/#537/#543 (universal pra qualquer endpoint com par half-open); shape JSON projeção MÍNIMA identificadora `{id, uid, summary, dtstart, dtend, rrule}` — sem `ical_raw`, `description`, `location`, `etag`, `sequence`, `organizer_email`, `created_at`, `updated_at` (UI faz follow-up `GET /:id` pra detalhe completo se quiser; campos selecionados são exatamente os necessários pra mostrar lista compacta + decidir se aciona bulk-update); `count` reflete tamanho do vetor PÓS-refilter (não o total não-paginado — UI percebe truncamento se `count == limit`); read-only, NÃO requer WRITE+; nome `/events-by-range` (path-level genérico) em vez de `/events-bulk-update/preview` ou `/events-bulk-delete/preview` foi escolha deliberada — preview de "quais eventos estão na janela" é compartilhado entre múltiplos bulk operations futuros (delete, update, move, set-rrule), agrupar tudo em UM endpoint genérico evita N endpoints de preview redundantes; paralelo do `overrides-by-range/preview` #543 mas a nível de master events (#543 era a nível de RECURRENCE-ID overrides DENTRO de um único master), confirmando o pattern hierárquico: cada nível de mutação (master events vs override children) tem sua própria família `*-by-range` + `/preview`; pattern "preview-first, mutate-after" agora aplicado proativamente ANTES do PATCH em massa existir — sprint #545+ traz `events-by-range` PATCH (mover, mudar calendar, set RRULE) reusando MESMO query critério do #544 com WRITE+ gate, e o preview do #544 já está pronto pra UI fazer dry discovery sem precisar montar body; insight final: pattern "preview-first" deve ser aplicado proativamente em endpoints de discovery genéricos (não específicos a uma operação mutativa) quando múltiplas mutações futuras compartilham o mesmo critério de seleção — single endpoint generaliza, evita N preview endpoints com lógica idêntica de seleção e shape de retorno divergindo só por nome — sprint #544 |
| #543 | calendar | overrides patch by-range dry-preview — `GET /api/v1/calendars/:cal_id/events/:id/overrides-by-range/preview?after=&before=` retorna o plano `{event_id, touched, skipped}` do que `PATCH /:id/overrides-by-range` afetaria, sem requerer body algum (sprint #543, complementa o `?dry=true` do #537 que ainda exige `PatchOverrideBody` válido com pelo menos 1 campo de mutação — UI agora pode "discovery" puro do universo afetado por um range antes de montar o body de patch); diferença chave filosófica vs `?dry=true` do #537: o universo de overrides afetados é INDEPENDENTE dos campos a patchar (afetar = match no range half-open `[after, before)`; o body só decide *o que* mudar, não *quem*) — então a validação `at_least_one_field` do #537 (que rejeita body vazio com 400) é desnecessária pra preview de quem; novo handler `patch_overrides_by_range_preview` + struct `PatchOverridesByRangePreviewQuery` (só `after`/`before`, sem `dry` que é implícito pelo método GET — paralelo aos GETs `/touch-preview` #514, `/exdates-preview` #525, sem flag dry redundante); rota `GET /:id/overrides-by-range/preview` registrada após o `PATCH /:id/overrides-by-range` na chain do `Router` — sub-path com `/preview` em vez de path completamente novo (e.g. `/preview-overrides-by-range`) preservando o agrupamento semântico "tudo do `overrides-by-range` em uma família de paths"; reusa walker `list_recurrence_id_overrides` + `parse_one_exdate` + classificação `[after, before)` half-open IDÊNTICA ao bloco `if dry {…}` do #537 (mesma lógica de bucketing, mesma dedup `touched.iter().any(...)/skipped.iter().any(...)` evitando duplicatas em compacts repetidos no walker); validação `after >= before` → 400 idêntica; master sem UID → 400 idêntica; diferença em comportamento "vazio": GET preview retorna 200 com `touched: []` (paralelo `touch-preview` que não 404 — preview é discovery, não action), enquanto `PATCH ?dry=true` retorna 404 com `EventNotFound` quando `touched` vazio (paralelo de "no overrides in range" pra mesmo tratamento de erro do path real do PATCH não-dry — UI não vê dry "ok" e real "fail"); shape JSON simétrico mas SEM `dry: true` flag (endpoint GET, "dry" é implícito; paralelo `touch-preview` que também não tem flag `dry: true`); read-only, NÃO requer WRITE+ (paralelo `touch-preview` #514, `exdates-preview` #525, todos os GETs `/preview*`); padrão consolidado: para QUALQUER endpoint mutativo com `?dry=true` query flag, idealmente existe um GET sibling `/preview` que NÃO requer body — flag dry é útil quando body validation passa antes de short-circuit (UI ainda monta body completo "for real" e só decide rodar ou não no último momento), mas GET preview é útil quando UI ainda NEM SABE qual body pretende usar e só quer descobrir o universo afetado por um range; pattern aplicável a outros endpoints PATCH-by-range futuros (e.g. quando vier "patch events by-range" no #544 candidato); insight final: a separação do universo afetado (range filter) vs do conteúdo da mutação (body fields) deve estar refletida na API — endpoints que mistura ambos forçam UI a montar body fictício pra discovery — sprint #543 |
| #542 | calendar | overrides stats `tzid_breakdown_by_kind` — `?include_kind_breakdown=true` em `GET /api/v1/calendars/:cal_id/events/:id/overrides/stats` emite o objeto adjacente `tzid_breakdown_by_kind: {tz: {tzid: N, unknown: M}}` particionando o count de cada TZID retido em duas sub-categorias: tokens cujo `dtstart_value`/`dtend_value` parseia como `YYYYMMDDTHHMMSS` local datetime (canônico, contado como `tzid`) vs malformados (e.g. `20991301T256000`, contado como `unknown`); sprint #542, paralelo direto do #536 mas no overrides scope — fechando o port COMPLETO das 5 famílias de flags do EXDATE-stats em overrides_stats: inclusão #538 (base agregação), head truncation #539 (`top_tzid` + `tzid_other_count`), cosmetic ordering #540 (`sort_tzid` + `tzid_breakdown_order`), tail filtering #541 (`min_count` + `tzid_filtered_count`), e agora kind drill-down #542 (`include_kind_breakdown` + `tzid_breakdown_by_kind`) — paralelismo arquitetural cross-resource agora 100% concluído, overrides_stats tem feature parity com EXDATE-stats; novo campo `include_kind_breakdown: Option<bool>` em `OverridesStatsQuery` com `#[serde(default)]` (paralelo direto do #536 cross-stats); diferença filosófica vs `by_kind` do #522 (taxonomia central de EXDATEs em utc/tzid/date-only/unknown onde `unknown` significa TZID ausente): aqui `unknown` é uma SUB-categoria DENTRO do bucket "TZID presente" — o item já tem `dtstart_tzid=Some(...)` em `list_recurrence_id_overrides` mas o token local pós-colon (`dtstart_value`) não é um datetime canônico parseável; reusa o helper top-level `is_canonical_local_datetime(&str) -> bool` introduzido em #536 (NÃO duplicar — single source of truth pra "datetime local válido", aplicável a qualquer call site que valide canonicalidade fora do walker central); decisão arquitetural não-óbvia: tive que ESTENDER o parser `list_recurrence_id_overrides` adicionando 2 novos campos no JSON Object dos items: `dtstart_value: Option<String>` e `dtend_value: Option<String>` capturando o token POST-COLON de linhas `DTSTART;TZID=…:VALUE` / `DTEND;…:VALUE` (até #538 o parser só capturava o TZID via `cur_dtstart_tzid` mas DESCARTAVA o value pós-colon — informação necessária pra validação canonical foi recuperada apenas para esse drill-down); CRÍTICO: `dtstart_value`/`dtend_value` são SEPARADOS de `dtstart`/`dtend` (que continuam sendo populados SÓ por linhas `DTSTART:` plain UTC compact sem params) preservando 100% backward-compat dos filtros qualitativos `has_dtstart`/`has_dtend` do #518/#521 — uma linha `DTSTART;TZID=Europe/Berlin:20260101T120000` continua sendo `present("dtstart") == false` (mantém invariants #519/#521), mas agora também tem `dtstart_value=Some("20260101T120000")` e `dtstart_tzid=Some("Europe/Berlin")` pra consumo pelo drill-down; campos novos são aditivos no GET list de overrides também (`/overrides` reusa `list_recurrence_id_overrides`) — clientes existentes ignoram, expõe metadado útil que antes ficava oculto, cross-endpoint consistente com a ideia "stats agrega o que list expõe" (paralelo do efeito colateral aditivo do #538 com `dtstart_tzid`/`dtend_tzid`); novo accumulator `tzid_by_kind: Vec<(String, usize, usize)>` mantido em paralelo a `tzid_breakdown: Vec<(String, usize)>` no MESMO loop de counting do #538 — invariant testável `canonical + malformed == tzid_breakdown[tz]` preservada por garantia estrutural (mesma chave incrementada em lockstep, paralelo do pattern #536); só populado quando `include_kind_breakdown=true` (zero overhead em items normais ou flag ausente — gate `if include_kind_breakdown` envolve TODA a sub-classificação); o loop foi refatorado de `for key in ["dtstart_tzid", "dtend_tzid"]` (do #538) pra `for (tzid_key, value_key) in [("dtstart_tzid", "dtstart_value"), ("dtend_tzid", "dtend_value")]` — pareando keys de TZID e value lado-a-lado pra que o lookup `item.get(value_key)` retorne o token canônico do MESMO campo (DTSTART ou DTEND) cujo TZID acabou de ser contado; lookup defensivo: se `item.get(value_key)` for null (improvável: parser sempre seta em pares com TZID, mas defensivo), `is_canonical_local_datetime` aplicado via `.map(...).unwrap_or(false)` faz o token contar como `unknown` (consistente com pattern do #536 onde malformed é o fallback seguro); aplicado APÓS toda a chain de presentation (`min_count` → `top_tzid` → `sort_tzid`) — só os TZIDs no `kept` final aparecem no `tzid_breakdown_by_kind`, na MESMA ordem alfabética default do JSON Object (a ordem custom continua só em `tzid_breakdown_order` do #540 — não duplicada num `*_order_by_kind`, paralelo da decisão do #536); lookup por TZID via `iter().find()` no `tzid_by_kind` durante emissão (linear, mas N pequeno e só executa quando flag set, paralelo do #536); shape final emitido `{tz: {tzid: N, unknown: M}}` para cada TZID em `kept`; ordem do payload final pós-#542: `tzid_breakdown` → `tzid_token_count` → `tzid_other_count` (se top_tzid set) → `tzid_filtered_count` (se min_count set) → `tzid_breakdown_order` (se sort_tzid set) → `tzid_breakdown_by_kind` (se include_kind_breakdown=true) — `*_order` continua antes de `*_by_kind` (paralelo da ordem #536); preserva 100% backward-compat: ausência da flag (None ou explicit `false`) NÃO emite o campo; `include_kind_breakdown=true` mas `kept` vazio (e.g. todos overrides filtraram-out por range/qualitativo do #520/#521 ou nenhum tem TZID) emite `tzid_breakdown_by_kind: {}` (mesma semantics "flag aceita, sem dados" do `tzid_other_count` quando `breakdown.len() <= n`); composição completa preservada com TODOS os flags anteriores — `?after=...&before=...&has_summary=true&min_count=2&top_tzid=10&sort_tzid=name_asc&include_kind_breakdown=true` é uma chain válida produzindo "top-10 dos TZIDs canônicos+malformados em overrides com summary editado pós-filter range, com count>=2, ordenados alfabeticamente, com sub-classificação por canonicidade do datetime local"; read-only, NÃO requer WRITE+; padrão consolidado: limitação de informação no parser (value pós-colon descartado quando params presentes) gerou solução não-invasiva — adicionar campos `*_value` paralelos aos `*_tzid` SEM tocar nos campos legados (`dtstart`/`dtend`) que têm semantics carregada pelos filtros qualitativos; mais geral: parsers que descartam informação devem ser estendidos via NOVOS campos (não modificação dos existentes) quando feature futura precisa do dado descartado, evitando ripple semantic em filtros downstream; insight cross-resource final: as 5 famílias de flags ortogonais sobre agregação JSON Object (inclusão, truncamento de cabeça, filtragem de cauda, ordenação cosmética, drill-down por kind) são agora um padrão arquitetural CONSOLIDADO replicável em qualquer endpoint stats que tenha agregação por chave dinâmica — futuras agregações (por user_id active/inactive, calendar_id shared/private) devem implementar TODAS as 5 famílias num roadmap previsível desde o sprint da agregação base — sprint #542 |
| #541 | calendar | overrides stats `min_count` — `?min_count=N` em `GET /api/v1/calendars/:cal_id/events/:id/overrides/stats` filtra long-tail removendo do `tzid_breakdown` qualquer TZID com `count < N` + adiciona `tzid_filtered_count: usize` agregando soma das counts removidas (sprint #541, paralelo direto do #535 mas no overrides scope — terceira presentation flag depois de `top_tzid` #539 e `sort_tzid` #540, completando o trio "truncate cabeça + ordenar + filtrar cauda" cross-resource sobre `tzid_breakdown` do overrides_stats); semantica filosóficamente DUAL ao `top_tzid`: top_tzid trunca CARDINALIDADE da CABEÇA (top-N por count desc), min_count filtra LONG-TAIL da CAUDA (TZIDs raros) — combinados oferecem janela arbitrária no histograma (`min_count=2&top_tzid=10` = "top-10 entre TZIDs com pelo menos 2 ocorrências cada", recortando ambos extremos do histograma simultaneamente); novo campo `min_count: Option<usize>` em `OverridesStatsQuery` com `#[serde(default)]` (mesmo padrão do `top_tzid`/`sort_tzid` #539+#540); validação `min_count == Some(0)` → 400 ("min_count must be >= 1 (omit flag for full breakdown)") com mensagem IDÊNTICA ao #535 cross-endpoint — semantics não varia contextualmente; `min_count=1` aceito mesmo sendo no-op (todo TZID no breakdown apareceu pelo menos 1x por construção — primeira fronteira "real", UI pode emitir `tzid_filtered_count=0` como confirmação que cauda está vazia); composição em 3 fases (ordem fixa, escolhida pra preservar semantics intuitiva, replicada idêntica do #535 EXDATE-stats no overrides scope): (1) `min_count` filtra long-tail removendo TZIDs raros do `Vec<(String, usize)>` original ANTES do truncate; (2) `top_tzid` seleciona top-N por count desc do UNIVERSO FILTRADO (não do original — composição compostável: `top_tzid=5&min_count=10` = "top-5 entre TZIDs com count>=10", se filtered retém 7 e top_tzid=5 trunca pra 5 com `tzid_other_count`=soma das 2 descartadas pós-filtro); (3) `sort_tzid` ordena o set retido (presentation, do #540) — chain `min_count → top_tzid → sort_tzid` é "filter universe → select head → present"; novo campo `tzid_filtered_count: usize` no JSON paralelo do `tzid_other_count` do #539 — mas reportando o que foi excluído pela CAUDA (long-tail filtrada por min_count) em vez da CABEÇA truncada (excedente do top-N) — emitido SOMENTE quando `min_count.is_some()` (mesmo que filtered_count=0 — UI sabe que flag foi aceita); implementação cria nova binding `(filtered_universe, tzid_filtered_count)` ANTES do `(kept, tzid_other_count)` — `filter` por `*c < n` na branch removed (acumula soma) e `filter` por `*c >= n` na branch kept (move o `Vec` original consumindo) com `into_iter()` — depois `top_tzid` opera no `filtered_universe` clonado (necessário porque move pra branch sem flag e clone na branch top-N); `tzid_token_count` continua reportando o universo TOTAL pré-filter (invariant `sum(tzid_breakdown.values()) + tzid_other_count + tzid_filtered_count == tzid_token_count` quando ambas flags set, ampliando a invariant do #539 que era só com `tzid_other_count`); `tzid_filtered_count` é emitido APÓS o `tzid_other_count` na ordem do payload (via `as_object_mut().insert()` em sequência), mantendo consistência cross-resource com EXDATE-stats #535 onde `tzid_filtered_count` aparece logo após `tzid_other_count`; ordem final do payload pós-#541: `tzid_breakdown` → `tzid_token_count` → `tzid_other_count` (se top_tzid set) → `tzid_filtered_count` (se min_count set) → `tzid_breakdown_order` (se sort_tzid set) — `*_order` continua sendo o ÚLTIMO campo emitido (paralelo #534/#540); composto naturalmente com filtros do #520/#521 (`after/before/has_summary/has_dtstart/has_dtend`): min_count atua APÓS a agregação que já é filter-aware, então `?has_summary=true&min_count=2` = "TZIDs com pelo menos 2 ocorrências entre overrides com summary editado"; read-only, NÃO requer WRITE+; padrão consolidado: trio (top_tzid #539, sort_tzid #540, min_count #541) agora forma o conjunto core das presentation flags em overrides_stats — falta só `tzid_breakdown_by_kind` (#542 candidato, paralelo do #536) pra fechar o port completo das 5 famílias do EXDATE-stats; insight: pattern arquitetural cross-resource agora prova ser robusto — agregações em JSON Object por chave dinâmica em qualquer endpoint stats devem oferecer 4 famílias de flags ortogonais (truncamento da cabeça `top_X`, ordenação cosmética `sort_X`, filtragem da cauda `min_count` + invariant compartilhada `sum(visible) + other + filtered == token_count`, e drill-down opcional `*_by_kind` quando há sub-classificação semantically relevante) — sprint #541 |
| #540 | calendar | overrides stats `sort_tzid` — `?sort_tzid=count_desc\|count_asc\|name_asc\|name_desc` em `GET /api/v1/calendars/:cal_id/events/:id/overrides/stats` ordena o `tzid_breakdown` via array adjacente `tzid_breakdown_order: ["tz_a", "tz_b", ...]` (sprint #540, paralelo simétrico do #534 mas no overrides scope — segunda presentation flag depois do `top_tzid` #539, continuando port das 4 famílias de presentation flags do EXDATE-stats #532+#533+#534+#535 pra overrides_stats); por que array adjacente em vez de mudar `.insert()` order: `serde_json::Map` SEM feature `preserve_order` (não ativada no workspace `Cargo.toml`, só `serde_json = "1"` plain) usa `BTreeMap` interno e serializa Object em ordem alfabética DETERMINÍSTICA — então alterar a ordem dos `.insert()` no `breakdown_obj` NÃO afeta o JSON output (descoberta original do #534, replicada idêntica aqui); única forma de transmitir ordem custom em JSON Object é via array adjacente `tzid_breakdown_order: [keys...]` que UI itera buscando counts em `tzid_breakdown` por chave; novo campo `sort_tzid: Option<String>` em `OverridesStatsQuery` com `#[serde(default)]` (paralelo direto do `sort_tzid` em ExdatesPreviewQuery/ExdatesStatsQuery #534); validação match 4-arm contra `count_desc|count_asc|name_asc|name_desc` → `Some(s @ ...)` reaproveita binding com guards alinhados, `None|Some("")` → None skip, outros → 400 com mensagem "sort_tzid must be one of count_desc|count_asc|name_asc|name_desc, got '{other}'" (mesma estrutura do #534 cross-endpoint — semantics IDÊNTICA, sem variação contextual diferente do #532 vs #533); aplicado em DUAS fases compostas com `top_tzid` no `Vec<(String, usize)>` `kept`: (1) selecionar top-N por count desc preserva semantics original do #539 (top-N selection precede sort cosmético); (2) re-ordena o set retido pelos N elementos via `match mode` 4-arm — `count_desc` → `kept.sort_by(\|a, b\| b.1.cmp(&a.1))`, `count_asc` → `a.1.cmp(&b.1)`, `name_asc` → `a.0.cmp(&b.0)`, `name_desc` → `b.0.cmp(&a.0)` (sort_by mutável no `kept` em vez de clone — diferente do #534 que sortou por valor; aqui a Vec já é local pós-truncate e não há razão pra clonar antes do sort cosmético, mais idiomático); emite `tzid_breakdown_order: serde_json::Value::Array` adjacente ao `tzid_breakdown` SOMENTE quando `sort_mode.is_some()` — flag ausente preserva 100% shape do #539 (sem array adjacente, Object em ordem alfabética default do BTreeMap); composto com `top_tzid=10&sort_tzid=name_asc`: array tem 10 TZIDs em ordem alfabética dos top-10 por count (NÃO os 10 alfabeticamente primeiros do universo total — top-N selection precede sort cosmético, mesma semantics do #534); `count_desc` é o comportamento default natural pós-truncate do #539 mas torná-lo explícito via flag dá UI chance de gerar `tzid_breakdown_order` mesmo quando `top_tzid` não é dado (ordenar breakdown completo por count em ordem decrescente — útil pra UI dashboard que quer count-rank visível mesmo no breakdown completo); `count_asc` resolve "least-frequent first" útil pra audit "qual o long-tail de TZIDs raras" em overrides (filter dual ao top_tzid que mostra heads); `name_asc` é redundante quando JSON Object já entrega ordem alfabética grátis via BTreeMap, mas torna a ordem EXPLÍCITA (UI não precisa assumir) e permite combinar com `top_tzid` produzindo "top-N alfabeticamente" que NÃO é alcançável sem o pair; `name_desc` é o uso mais "exótico" (Z-A) — mantido pra paridade simétrica com #534 onde também existe; ordem do payload final: `tzid_breakdown` → `tzid_token_count` → `tzid_other_count` (se top_tzid set) → `tzid_breakdown_order` (se sort_tzid set), via `as_object_mut().insert()` em sequência (paralelo do pattern do #534 onde `*_order` é o ÚLTIMO campo emitido, depois de `*_other_count`); read-only, NÃO requer WRITE+; padrão consolidado: limitação técnica (`serde_json::Map` sem preserve_order) gera padrão arquitetural cross-resource — agregados em JSON Object com ordem custom usam array adjacente `*_order: [keys...]` como contrato API estável independente da implementação interna do serializer; pattern aplicável a qualquer agregação por chave dinâmica (TZID em EXDATE-stats #534 + agora overrides-stats #540, futuras: user_id, calendar_id) que precise de ordem custom — sprint #540 |
| #539 | calendar | overrides stats `top_tzid` — `?top_tzid=N` em `GET /api/v1/calendars/:cal_id/events/:id/overrides/stats` trunca o `tzid_breakdown` do #538 pras N TZIDs mais frequentes (sort by count desc, ties broken por insertion order do `Vec` association list — ordem de aparição nos overrides retidos pós-filtro range/qualitativo do #520/#521) + adiciona `tzid_other_count: usize` agregando soma das counts das TZIDs descartadas (sprint #539, paralelo direto do #532+#533 mas no overrides scope — primeira presentation flag depois da base #538, abrindo a sequência de port das 4 famílias de presentation flags consolidadas em EXDATE-stats #532+#533+#534+#535+#536); novo campo `top_tzid: Option<usize>` em `OverridesStatsQuery` com `#[serde(default)]` — flag opcional, ausência (None) preserva 100% shape do #538 (breakdown completo, sem `tzid_other_count`); validação `top_tzid=Some(0)` → 400 ("top_tzid must be >= 1 (omit flag for full breakdown)") — mesma mensagem do #535/#534 onde semantics é uniforme (não há alternativa "agregado sem breakdown" em overrides_stats que justifique mensagem contextual diferente como #532 vs #533); implementação IDÊNTICA ao #532+#533: clona `tzid_breakdown: Vec<(String, usize)>` original (preserva insertion order pra branch sem flag), `sort_by(|a, b| b.1.cmp(&a.1))` desc, soma `iter().skip(n).map(|(_, c)| *c).sum()` pra `other_count`, `truncate(n)` no clone — O(N log N) com N=tzids distintos, irrelevante na prática (cardinalidade típica baixa em overrides porque cada override é manualmente editado, raramente há mais de 5-10 TZIDs distintos por evento); aplicado APÓS o full breakdown ser construído no loop de counting do #538 (NÃO dentro do loop) — separação limpa entre acumulação (#538) e apresentação (#539), single-responsibility (paralelo da estrutura #531 vs #533 onde também são fases distintas); `tzid_token_count` continua reportando o TOTAL pré-truncate (invariant `sum(tzid_breakdown.values()) + tzid_other_count == tzid_token_count` quando `top_tzid` é set, paralelo do behaviour do #533 — `tzid_token_count` reflete UNIVERSO total da agregação, não o subset pós-truncate apresentado); pequeno refactor de extração: o loop de soma `for (tz, c) in &tzid_breakdown { breakdown_obj.insert(...); tzid_token_count += *c; }` do #538 foi separado em duas fases (primeiro acumula `tzid_token_count` do universo total via `for (_, c) in &tzid_breakdown { tzid_token_count += *c; }`, depois aplica truncate condicional, depois itera `kept` pra `breakdown_obj`) — necessário porque `tzid_token_count` deve refletir o universo total mesmo após truncate (UI pode comparar `tzid_token_count - sum(visible) == tzid_other_count`); usa `serde_json::json!({...})` mutável + `.as_object_mut().unwrap().insert("tzid_other_count".into(), ...)` condicional em vez de `serde_json::json!` literal com campo opcional (mais conciso que duas variantes de macro completas, paralelo do pattern usado em sprints anteriores quando há campo opcionalmente emitido); composto naturalmente com filtros do #520/#521 (`after/before/has_summary/has_dtstart/has_dtend`): top_tzid atua APÓS a agregação que já é filter-aware, então `?has_summary=true&top_tzid=5` = "top-5 TZIDs entre overrides com summary editado"; read-only, NÃO requer WRITE+; padrão consolidado: pattern "agregação base primeiro #538, presentation flags depois #539+" agora replicado de EXDATE-stats pra overrides — próximas sprints (#540 sort_tzid, #541 min_count, #542 by_kind opcionalmente) completam o port das 4 famílias de flags ortogonais sobre `tzid_breakdown` do overrides_stats; insight: pattern "ranking-truncado + bucket residual" (`top_X=N` + `X_other_count`) consolidado como genérico cross-endpoint cross-resource — agora aplicado em 3 endpoints distintos (exdates-preview-stats #532, exdates-stats #533, overrides-stats #539), demonstrando que TODOS stats endpoints com agregação por chave dinâmica devem oferecer essa flag — sprint #539 |
| #538 | calendar | overrides stats `tzid_breakdown` base — `tzid_breakdown: {"Europe/Berlin": N, "America/Sao_Paulo": M, ...}` em `GET /api/v1/calendars/:cal_id/events/:id/overrides/stats` agrega ocorrências de TZID em `DTSTART;TZID=…` E `DTEND;TZID=…` dos overrides retidos pós-filtros range/qualitativos do #520/#521 (sprint #538, paralelo do #530+#531 mas no overrides scope — primeira agregação por dimensão dinâmica TZID em overrides_stats que até então só tinha `by_field`/`by_category` qualitativos do #519); pré-requisito do futuro port do `tzid_breakdown_by_kind` #536 — esta sprint introduz SÓ a base SEM flags top_tzid/sort_tzid/min_count/by_kind (consistente com #530 que também foi base apenas, follow-on flags chegaram em #532+); cada override pode contribuir 0/1/2 TZIDs — DTSTART e DTEND contados INDEPENDENTEMENTE; se ambos têm a MESMA TZID, breakdown[tz] += 2 (se forem diferentes, cada conta 1) — semantics "cada token TZID nos overrides retidos = 1 ocorrência", paralela ao #530 que conta cada EXDATE com TZID como 1; novo campo `tzid_token_count: usize` no payload preservando invariant testável `sum(tzid_breakdown.values()) == tzid_token_count` (paralelo da invariant `sum == non_utc_by_kind.tzid` do #530 — útil pra cliente verificar consistência sem somar manualmente); extensão de `list_recurrence_id_overrides` adiciona 2 campos novos no JSON Object dos items: `dtstart_tzid: Option<String>` e `dtend_tzid: Option<String>` extraídos via novo helper inline closure `parse_tzid_from_params(&str)` que aplica `kv.split(';').filter(...)` no segmento ENTRE property-name e ':' (handles params arbitrários antes/depois de TZID, case-insensitive na chave `TZID=`, value preservado case-original — IANA TZIDs são case-sensitive de jure mas case-insensitive na prática maior; preferi preservar literal); novos branches `DTSTART;`/`DTEND;` (com `;` literal, distintos dos branches `DTSTART:`/`DTEND:` existentes) — IMPORTANTE: NÃO setam `cur_dtstart`/`cur_dtend` (esses continuam SÓ em linhas `DTSTART:` plain UTC compact), preservando 100% backward-compat dos filtros qualitativos `has_dtstart`/`has_dtend` do #518 (uma linha `DTSTART;TZID=...:` continua sendo "absent" pra has_dtstart, mantendo invariants do #519/#521); accumulator `tzid_breakdown: Vec<(String, usize)>` association list IDÊNTICA ao #530+#531 (não HashMap — N TZIDs distintos típico baixo, lookup linear `iter().position()` mais barato que hash) populada DENTRO do loop de counting do `by_field`/`by_category` (single-pass, paralelo da estrutura otimizada do #531 que também acumula no mesmo loop); serializado via `serde_json::Map::new()` + `.insert()` em vez do macro `json!` pra suportar chaves dinâmicas (TZID strings — runtime values, não literals compile-time, paralelo do #530+#531); shape final `{event_id, total, by_field, by_category, tzid_breakdown, tzid_token_count}` — `tzid_breakdown` SEMPRE presente mesmo que vazio `{}` (diferente do #530 que omite junto com `non_utc_by_kind` quando `include_non_utc=false`/`only_utc=true` — overrides_stats não tem flag de inclusão equivalente, então `tzid_breakdown` sempre emitido como list-stats #531); ordem de iteração = ordem de aparição (estável pra mesma input mas não lexicográfica — UI sortear se quer ordem alfabética, paralelo do #530); efeito colateral aditivo no GET list de overrides (`/overrides`) que reusa `list_recurrence_id_overrides`: agora também emite `dtstart_tzid`/`dtend_tzid` no JSON dos items — campos novos são aditivos (clients existentes ignoram), expõe metadado útil que antes ficava oculto, cross-endpoint consistente com a ideia "stats agrega o que list expõe"; flags existentes do #520/#521 (`after/before/has_summary/has_dtstart/has_dtend`) continuam funcionando IDÊNTICAS — agregação `tzid_breakdown` é APÓS retains, portanto naturalmente filter-aware (paralelo da semantics do #531 onde `tzid_breakdown` é populada pós-retains do `kind=&with_tzid=&with_params=` etc); composição completa preservada — `?after=...&before=...&has_summary=true` agrega TZIDs só dos overrides retidos no subset qualitativo+temporal; read-only, NÃO requer WRITE+; insight: pattern "agregação primeiro, presentation depois" (consolidado em #530→#532+#533+#534+#535) agora portado pra overrides — sprints futuros de overrides_stats devem seguir mesma sequência (top_tzid → sort_tzid → min_count → tzid_breakdown_by_kind), permitindo que toda a infraestrutura de flags ortogonais do EXDATE-stats (5 famílias: inclusão, truncate cabeça, filter cauda, sort cosmético, kind drill-down) seja eventualmente paralelizada no overrides_stats — sprint #538, abre espaço pro port direto do #536 (`tzid_breakdown_by_kind`) num sprint futuro |
| #537 | calendar | overrides patch by-range — `PATCH /api/v1/calendars/:cal_id/events/:id/overrides-by-range?after=&before=&dry=` aplica mutação real (summary/description/location/dtstart/dtend) em massa a TODOS os overrides cujo RECURRENCE-ID cai em `[after, before)` (sprint #537, paralelo direto do touch-by-range #509 mas com payload de mutação em vez de só DTSTAMP — fechando o trio bulk-by-range "touch (#509) + dry (#512) + patch real (#537)"); body é `PatchOverrideBody` REUSADO do #498 (single-patch override) — pelo menos 1 dos 5 campos obrigatório, mesma validação 400 "at least one of summary/description/location/dtstart/dtend required" replicada em ambos endpoints; semantics: TODOS os overrides match ganham o MESMO patch (não payload-per-override — pra granularidade fine-grained o user usa #498 single-patch endpoint-by-endpoint); chassi idêntico ao `touch_overrides_by_range` (#509+#512): half-open range `[after, before)`, ambos opcionais (≡ patch em massa de TODOS overrides), validação `after >= before` → 400 (mesma msg cross-handler), descoberta via `list_recurrence_id_overrides`, parse de `compact` via `parse_one_exdate`, retains de range filter, dedup via `iter().any()` em ambos `touched`/`skipped`, `patch_recurrence_id_override_block` per-match acumulando em `&mut raw`, 1 único `EventRepo::update` no fim, mesma `EventUpdated` event publicação; diferença chave vs #509: aqui o helper recebe `body.summary.as_deref()`/`body.description.as_deref()`/etc em vez de `None×5` — o mesmo helper de #498 single-patch é reusado, demonstrando ortogonalidade do design (campos individuais opcionais via `Option<&str>` sem necessidade de variantes); `?dry=true` (paralelo simétrico do #512): mesma estrutura — só retorna `{dry:true, event_id, touched, skipped}` sem `EventRepo::update`, sem alterar ETag/`updated_at`/DTSTAMP, sem publicar `EventUpdated`; mesmas validações 400 (range invalid, body vazio, master sem UID) e 404 (touched vazio, EventNotFound) que path real — UI confirma "patch vai afetar N overrides em [after,before), N' fora, ok?" antes de rodar; HTTP method é `PATCH` (não POST como #509) — convenção REST: touch é "side-effect-only" (POST), patch real altera state semântico (PATCH); requer adicionar `patch` ao `routing::{...}` import (eram só `delete/get/post` antes); rota registrada em `/overrides-by-range` (path coerente com `/touch-overrides-by-range`); padrão arquitetural consolidado: cada endpoint single-resource em CRUD (GET/PATCH/DELETE em `/overrides/:recurrence_id`) tem dual `bulk-by-range` paralelo (`-by-range` suffix com `?after=&before=&dry=` query params) que reusa o helper interno do single-resource — touch foi o primeiro #505→#509+#512 (single + bulk + dry), patch agora segue mesma trajetória #498→#537 mas em UM sprint (não preciso de 3 separados: payload e dry já consolidados); sequence NÃO bumpa (consistente com #505/#506/#507/#508/#509: patches em overrides não tocam colunas master comparadas pelo `EventRepo::update`); ETag/`updated_at` refrescam (basta pra invalidar caches HTTP/CalDAV); requer WRITE+; insight: trio `single (#498) + bulk-by-range com mutação (#537) + dry-run (incorporado no #537)` agora completo — overrides têm cobertura paralela ao touch family que tem `touch single (#505) + bulk all (#508) + bulk-by-range (#509) + dry-run (#510/#511/#512/#513)`, demonstrando que toda família CRUD de overrides eventualmente ganha endpoints `-by-range` com query semantics consistente — sprint #537 |
| #523 | calendar | EXDATE list filter qualitativo — `?with_tzid=true\|false&with_params=true\|false` em `GET /api/v1/calendars/:cal_id/events/:id/exdates?detail=full` (sprint #523, paralelo simétrico do #518 mas pra EXDATE list — overrides têm presença binária por campo `summary/dtstart/dtend`, EXDATE tem presença binária por metadado `tzid/params`); 2 novos campos `with_tzid/with_params: Option<bool>` em `ListExdatesQuery` com `#[serde(default)]` (Option<bool> tem Deserialize natural, sem `with`); filtros aplicados dentro do mesmo closure `.filter()` do full branch (junto com `kind` filter do #516, AND-composto inline em vez de retain sequencial — operam sobre `ExdateInfo` antes do `.map()` pra JSON, mais idiomático que retain pós-collect); `true` exige campo presente (`info.tzid.is_some() == true`), `false` exige ausência (`info.tzid.is_some() == false`); independentes do `kind` filter — `with_tzid=true` é mais permissivo que `kind=tzid` porque captura TZID em formato malformado (kind=unknown) também, útil pra audit qualitativo "tem TZID em qualquer formato"; só fazem sentido com `detail=full` — em `summary` (que usa `parse_exdates` plain sem captura de tzid/params) → 400 explícito ("with_tzid/with_params filters require detail=full"); ausência total dos 2 flags preserva 100% shape do #516 (filter degenera num pass-through `if let Some(want)` que skip total quando `q.with_tzid.is_none()`); read-only, NÃO requer WRITE; padrão `(filter, agg)` agora completo no list endpoint do EXDATE também: `kind` (#516, categórico exclusivo) + `with_tzid/with_params` (#523, qualitativo binário) — paralelo direto do trio overrides {range #517, presença #518}; insight: assimetria estrutural entre overrides (campos `summary/dtstart/dtend` no override item) e EXDATE (metadados `tzid/params` na linha EXDATE) emerge naturalmente nos filters — overrides têm 3 flags `has_*` em #518, EXDATE tem 2 flags `with_*` em #523 — sprint #523 |

---

## Sessões anteriores (#333–#335)

| Sprint | Escopo | O que foi feito |
|--------|--------|-----------------|
| #333 | admin | Migration `updated_at` em `govbr_user_map` + trigger automático; `govbr.rs` usa `updated_at` direto para ETag/LM |
| #334 | search | `POST /api/v1/index/bulk` — indexa até 500 docs por chamada; um único commit Tantivy |
| #335 | compliance | `Last-Modified` + `IMS` em `list_policies`; ETag/LM via `updated_at` em `get_policy` |

---

## Próximos candidatos (#551-#555)

1. **calendar:** events by-range set-description — PATCH `/events-by-range/set-description?after=&before=&description=` paralelo DIRETO do #550 set-location (não do #549 set-summary) porque description também é nullable per RFC 5545 §3.8.1.5 — mesmo template `Option<&str>` no repo, política "empty=clear" idêntica ao #550; ripple no FTS `#461` similar ao #549/#550 (description costuma ter peso médio no scoring); coluna `description` autoritativa pra API estruturada, raw stale até próximo PUT
2. **calendar:** events by-range set-organizer-email — PATCH `/events-by-range/set-organizer-email?after=&before=&organizer_email=` similar ao #550 mas com semantics distinta: organizer é metadata de PROPRIEDADE/ATTENDEE em VCALENDAR (RFC 5545 §3.8.4.3) e mudar em massa pode ter implicações iTIP (#491) não-triviais; possivelmente WAY mais cuidadoso — defer pra depois das set-{simple-text} ou aceitar como simples bulk-set documentando que iTIP outbound não é re-disparado
3. **calendar:** events by-range set-rrule (com validação) — PATCH `/events-by-range/set-rrule?after=&before=&rrule=` reusando critério do #544/#546-#550; aceita RRULE válida (paralelo da validação do `EventRepo::update`) e aplica em massa; mais complexo que #547-#550 porque precisa validar a string RRULE antes do UPDATE; trade-off: re-parsear cada `ical_raw` (custoso) OU adotar mesmo trade-off do #547-#550 ("coluna fresh, raw stale")
2. **search:** adicionar `received_at` ao tantivy schema + facet temporal (sprint maior, requer reindex)
3. **meet:** participant invite via mail real — chamada cross-service usando `reqwest`
4. **mail:** sieve filter test endpoint — `POST /api/v1/mail/sieve/test`
5. **drive:** trash auto-purge schedule — config tenant pra auto-rodar #453 periodicamente
6. **mail:** folder rename revert-by-mailbox — `POST /folders/rename-history/by-mailbox/:mailbox_id/undo` (granular variant de #490)
7. **drive:** tag intersect-exclude por user — variant user-scoped de #489 com filtro `created_by`

---

## Arquitetura resumida

| Serviço | Porta | Arquivo principal |
|---------|-------|-------------------|
| expresso-mail | 8001 | `services/expresso-mail/src/` |
| expresso-flows | 8005 | `services/expresso-flows/src/main.rs` |
| expresso-compliance | 8009 | `services/expresso-compliance/src/main.rs` |
| expresso-notifications | 8007 | `services/expresso-notifications/src/` |
| expresso-contacts | 8003 | `services/expresso-contacts/src/` |
| expresso-calendar | 8004 | `services/expresso-calendar/src/` |
| expresso-chat | 8006 | `services/expresso-chat/src/` |
| expresso-drive | 8008 | `services/expresso-drive/src/` |
| expresso-admin | 8010 | `services/expresso-admin/src/` |
| expresso-meet | 8011 | `services/expresso-meet/src/` |
| expresso-search | 8013 | `services/expresso-search/src/` |
| expresso-imap | 993/143 | `services/expresso-imap/src/` |
| expresso-smtp | 25/587 | `services/expresso-smtp/src/` |

**Shared libs:** `expresso-core` (DB pool, migrations, AppConfig), `expresso-auth-client` (OIDC/JWT), `expresso-observability` (Prometheus metrics router)

---

## Padrões usados recorrentemente

```rust
// MAX(field) + IMS check antes do rows query (list handlers)
let max_ts: Option<OffsetDateTime> = sqlx::query_scalar(
    "SELECT MAX(updated_at) FROM tbl WHERE tenant_id = $1 AND user_id = $2",
)
.bind(ctx.tenant_id).bind(ctx.user_id)
.fetch_one(pool).await.unwrap_or(None);

if let Some(ts) = max_ts {
    if let Some(ims_val) = req_headers.get(header::IF_MODIFIED_SINCE) {
        if let Ok(ims_str) = ims_val.to_str() {
            if let Ok(ims_dt) = OffsetDateTime::parse(ims_str, &time::format_description::well_known::Rfc2822) {
                if ts <= ims_dt { return Ok(StatusCode::NOT_MODIFIED.into_response()); }
            }
        }
    }
}
// ... rows query ...
let mut resp = (...).into_response();
if let Some(ts) = max_ts {
    let lm = ts.format(&time::format_description::well_known::Rfc2822).unwrap_or_default();
    resp.headers_mut().insert(header::LAST_MODIFIED, HeaderValue::from_str(&lm).unwrap());
}

// ETag + LM + IMS em get_one
let etag = format!("\"{}-{}\"", resource.updated_at.unix_timestamp(), resource.id);
if let Some(inm) = req_headers.get(header::IF_NONE_MATCH) {
    if inm.as_bytes() == etag.as_bytes() { return Ok(StatusCode::NOT_MODIFIED.into_response()); }
}
let lm = resource.updated_at.format(&time::format_description::well_known::Rfc2822).unwrap_or_default();
if let Some(ims_val) = req_headers.get(header::IF_MODIFIED_SINCE) { ... IMS check ... }
let mut resp = Json(resource).into_response();
resp.headers_mut().insert(header::ETAG, HeaderValue::from_str(&etag).unwrap());
resp.headers_mut().insert(header::LAST_MODIFIED, HeaderValue::from_str(&lm).unwrap());

// bulk_index (search)
// POST /api/v1/index/bulk  body: {"documents": [IndexDoc, ...]}  (max 500)
// resposta: {"indexed": N, "rejected": ["doc_id_invalido", ...]}

// COUNTER proposals accept (calendar)
let new_raw = itip::apply_proposed_times(&event.ical_raw, prop.proposed_dtstart, prop.proposed_dtend)?;
erepo.update(ctx.tenant_id, event.id, &new_raw).await?;
crepo.resolve(ctx.tenant_id, id, "accepted", Some(ctx.user_id)).await?;
```

---

## Instrução para continuar

```
execute all next phase tasks with no prompt.
```

Claude lê `memory/project_status.md` automaticamente e retoma do sprint seguinte.
